# 知行读书 — Agent Guide

> **面向对象**：所有 AI Agent（Claude Code / Cursor / Continue / Trae）
> **生效日期**：2026-07-20
> **配套规范**：[CLAUDE.md](CLAUDE.md) + [.learnings/LEARNINGS.md](.learnings/LEARNINGS.md) + [.learnings/PROGRESS.md](.learnings/PROGRESS.md)
> **最近核验**：2026-09-25（**复扫 IPC 通道的渲染层消费者：砍掉 25 条零调用通道，并当场抓到一条真断链**：`admin:getPrompts` 有 preload 暴露、有页面在调、主进程**没人注册** ⇒ 后台「提示词中心」一进去就是 reject（`getAllAdminPrompts()` 一直是好的，只差一行 `handle()`）；新增 `tests/ipc-channel-wiring.test.ts` 钉住「preload invoke 的每条通道必须有 handle」「声明表里每条通道要么注册要么推送」，通道 168→**143**（含被上一批备份改造变成孤儿的 `cards:createForExisting`）；**顺手踩到自己一条坑**：批量删通道时 `IPC_CHANNELS.VOCABULARY.CREATE` 按子串匹配把 `CREATE_FROM_LOOKUP` 的 handler 一起删了（四个页面在调），补回后把这个形状写进守卫的反证。`npm run verify` 退 0 = **84 文件 / 1279 用例**；同日更早（**备份要有下文：恢复完界面重新加载，多久没备份了一句话说得准**：上一批把备份做完整之后留了两个尾巴 —— ① 恢复是整批替换，可界面上每个 store 里挂的还是旧库的数据（原先只手动刷了设置页那三个 KPI，其他页面要到下次进入才更新），现在恢复完成报完数就整页重新加载，并由 `tests/data-io-import.test.ts` 钉住"没点确认绝不动手 / 先报数再重载 / 主进程报错就不重载"；② 新增 `src/shared/backup-reminder.ts`（纯判定：从没备份过说一句、超过 30 天说「上次备份是 X · 已经 N 天」、刚备份过一个字不说，时钟跳到过去按刚备份过处理），通知面板接上这一行，点它去「设置 → 数据」，不挂红点不弹窗。`npm run verify` 退 0 = **83 文件 / 1274 用例**（覆盖率清单收了 `use-data-io.ts` ⇒ 聚合如实从 89.27 降到 88.29，被统计文件 54→56）；同日更早（**备份终于对得起「完整备份」这句话**：表清单收成 `src/shared/backup.ts` 一份（15 张业务表 + 2 张写明理由的排除表），取数与恢复都收进主进程 `electron/services/backup.ts`，恢复走单个事务；此前备份由渲染层拼 6 次 IPC、**导出了 `cards` 却从不读它**（只按划线重建复习卡 ⇒ 知识卡片/方法论的「加入复习」登记恢复后全丢），而 AI 生成的层级摘要与 `ai_generation_batches` 台账**根本不在清单里**（恢复后台账归零 ⇒ 按钮变「重新蒸馏」，同一批划线再花一次钱），对话历史也没带走过。通道 166→168。`npm run verify` 退 0 = **81 文件 / 1265 用例**；同日更早（**跨进程交回的行形状不再靠约定，渲染层硬转 23 → 3 处**：`book` / `highlight` / `methodology` / `knowledgeCard` 的读取通道声明改回"数据库那一行"（原来写的是映射**之后**的驼峰领域类型，于是每个页面都得补一层 `as unknown as` / `as unknown[]` 自己掰），16 处映射前的硬转随之消失；`fsrs:previewReviewRatings` 那条"蛇形驼峰都认"的分支删掉（渲染层唯一调用方只可能传驼峰）；复习记录导出的列清单钉在 `ReviewRow` 的键上（列名写错从"导出一列空白"变成编译不过）。`npm run verify` 退 0 = **80 文件 / 1256 用例**；同日更早（**拼进 SQL 的列名必须真是那张表的列**：四个 `update(id, record)` 的 record 由渲染层整包下发，键名过去原样拼进 `SET x = ?` —— 值走 `?` 占位符，列名没人管，`{ 'content = 1, note': 'x' }` 就能改写整条 SET 子句；现在建 SQL 之前先按 `PRAGMA table_info` 判，不合法整条不发。`npm run verify` 退 0 = **80 文件 / 1246 用例**；同日更早（**会话行类型不再与运行时不符 + 「两种写法都认」的死兜底清掉**：`conversations` / `chat_messages` 有了一行一形（`ConversationRow` / `ChatMessageRow` 走 IPC，界面用 `ConversationView` / `ChatMessageView`），`shared/types.ts` 里那两份驼峰的 `Conversation` / `ChatMessage` 删除；`daily_stats` / `reviews` 的驼峰别名读法（`readingTimeSeconds` 等三种拼法，无生产方）一并清掉，归一化函数改成收真实行类型；`npm run verify` 退 0 = **79 文件 / 1226 用例**；同日更早（**生词本与文章补上行映射层（Issue #3 做完）**：`vocabulary` / `articles` 两张表此前没有映射器，页面各自 `as unknown as` 硬转自己声明的行类型（同一个生词行三份声明，`is_mastered` 一份写 number 一份写 boolean），现在收进 `utils/db-mapper.ts` 一份并按真库列双向对账，渲染层硬转 32 → 23 处；`npm run verify` 退 0 = **79 文件 / 1218 用例**；同日更早（**AI 生成能分批续跑到整本书**：新增 `ai_generation_batches` 台账（第 17 张表）记「这一批喂过哪些划线」，界面上按书显示「已处理 60/329」与「继续剩余 269 条」，每次点击的花费不变；成品清空后台账自动作废；`npm run verify` 退 0 = **79 文件 / 1211 用例**，通道 164→166；同日更早（**AI 生成说清覆盖了整本书的多少**：知识卡片蒸馏与方法论提取过去各自硬截 60 / 50 条划线且界面上只说「完成」，现在上限收进 `src/shared/ai-coverage.ts` 一份、结果里带覆盖数、界面如实写出「本次只覆盖 60/312 条划线」；`npm run verify` 退 0 = **77 文件 / 1171 用例**，被统计文件 50；同日更早（**AI 生成的东西进复习队列**：知识卡片与方法论可「加入 / 移出复习队列」，`cards` 表由"只能挂划线"改成三种来源各一个可空外键列（老库整表重建迁移，实测开发库 934 张卡逐字无损）；通道 161→164；`npm run verify` 退 0 = **76 文件 / 1158 用例**，被统计文件 49；同日更早（**修掉「删一本书不级联」：sql.js 的 `export()` 会把 `PRAGMA foreign_keys` 复位成 0**，第一次落盘之后库里所有 `ON DELETE CASCADE` 就不再生效 ⇒ 落盘取完字节立刻重开外键 + 5 条走真实落盘的守卫用例；`npm run verify` 退 0 = **74 文件 / 1138 用例**，装机版与开发库实测孤儿行均为 0）；同日更早（功能深化批次：AI 引用回原文 / 出处显示 / 练习记账 / 上下文补文章与生词 / 删除可撤销等 8 项做完，`npm run verify` 退 0 = **73 文件 / 1133 用例**，通道 163→161（砍掉 5 条零消费者的物理删除、加 2 条撤销用）；⚠️ 同一轮在跑着的开发版上实测出**外键级联在运行期不生效**（删书 / 删划线留下孤儿行），已开任务跟；同日更早的工具链批次 A1：**Vite 5.4.21 → 6.4.3、electron-vite 2.3.0 → 5.0.0**（连带 esbuild 0.25.12，靠 electron-vite 5 自带，不用 overrides），告警 **10 → 6**；`npm run verify` 退 0 = **64 文件 / 1071 用例**、`npm run dev` 真起到主窗口、产物形态逐项核过；⚠️ **安装包这轮没能重出** —— 本机 electron-builder 下载不到自己的工具链二进制（缓存已空 + 到 GitHub 下载主机超时；换镜像则撞 `@electron/get` 的 `ElectronDownloadCacheMode` undefined），已用升级前的依赖树复现同一报错 ⇒ 与本次升级无关，所以「vite 6 打的包能正常启动」这句本轮**没说**，等出包通路修好再补。同日更早：文档指针批次收掉 6 处指向不存在文件的引用（Issue #7 关闭）+ `tests/doc-pointers.test.ts` 17 条守卫；覆盖率批次把阈值从装饰接成真门禁（Issue #9 关闭，CI 与 `verify` 都跑 `test:cov`，被统计文件 18 → 45）；质量批次把 `tests/` 纳入 typecheck（Issue #6 关闭，积压的 61 处类型错误清零）；2026-09-23 安全批次 2/2 + 1/2：**密钥原值不再跨进程下发**（渲染层只拿 `wereadApiKeySet` / `llmKeySet` 两个布尔，输入框「留空即不修改」、移除走显式清除，顺带修掉「只改模型不重填 key 就不下发」的断链并砍掉随之变死的 `WEREAD.SET_API_KEY`，通道 164→163）+ API 密钥**真正**接通系统加密存储（Issue #13 选「接通」）；再早：Electron → **39.8.10**、Vitest → **3.2.7**、electron-builder → **26.15.3**、开发版数据目录时序缺陷修复、GitHub 维护一轮、v1.3.4 出包并发布）

---

## 一、项目速览

**知行读书**（Zhixing Reader）— Electron 桌面应用，知行合一的阅读成长伙伴。

| 维度 | 详情 |
|------|------|
| 形态 | Electron 三进程桌面应用（Main / Preload / Renderer）|
| 框架 | electron-vite 5 + React 19 + TypeScript 5.6 strict |
| 存储 | sql.js (SQLite WASM) + 本地 BM25 检索索引（内存构建）|
| 核心能力 | 微信读书同步、FSRS 间隔重复、AI 智能体对话、知识卡片、词汇学习 |
| 打包 | electron-builder → Windows NSIS 安装包 |

---

## 二、目录结构（30 秒读懂）

```
zhixing-reader/
├── electron/              # Main 进程：DB、IPC、AI、FSRS、WeChat Read API
│   ├── main.ts            # 入口（窗口创建 + 初始化序列）
│   ├── preload.ts         # contextBridge API 暴露面
│   ├── ipc/               # IPC handlers（按领域 11 文件 + index.ts 统一注册 + types.ts 契约）
│   ├── database/          # sql.js DB（按领域 15 文件 + index.ts / schema.ts / connection.ts）
│   ├── fsrs-engine.ts     # FSRS-6.0 适配层（基于 ts-fsrs 5.4.1，对外 API 100% 兼容）
│   ├── agent/             # 智能体（意图分类 / 编排 / 策略）
│   └── services/          # 业务服务（RAG / 知识卡片 / Prompt 模板 / 启动修复）
│
├── src/renderer/          # Renderer 进程：React SPA
│   └── src/
│       ├── pages/         # 路由页面（Bookshelf / Review / Chat / Settings / ...）
│       ├── components/    # 通用 UI 组件（layout / chat / ui 等）
│       ├── stores/        # Zustand 状态管理（6 个 store）
│       ├── design/        # 设计系统
│       ├── utils/         # 渲染层工具
│       └── styles/        # Tailwind CSS
│
├── src/shared/            # 跨进程共享：类型 + IPC 通道常量
├── tokens/                # brand.json — 全部色值的唯一真值（DTCG）
├── brand/                 # 徽标唯一真值（mark*.svg / wordmark / logo-horizontal / grid + README 规范）
├── scripts/               # 构建期脚本（build-tokens.mjs、build-icons.mjs）
├── resources/             # 静态资源（dictionary.json / icon.png / icon.ico —— 后两者由脚本生成）
├── tests/                 # Vitest 单元测试（84 文件 / 1279 用例）
│
├── .learnings/            # 经验与进度沉淀（⚠️ 本地文件，.gitignore 排除，不入库）
│   ├── LEARNINGS.md       # 踩坑与最佳实践
│   └── PROGRESS.md        # 路线图与待办
├── .workbuddy/memory/     # 会话交接记录（⚠️ 本地文件，不入库）
├── .github/workflows/     # CI（lint + typecheck + test:cov + build）
├── docs/                  # 设计文档 + 调研报告（⚠️ .gitignore 排除，不入库）
│
├── AGENTS.md              # ← 你正在读的（所有 Agent 入口）
├── CLAUDE.md              # Claude Code 专属配置
└── package.json           # 依赖与脚本
```

---

## 三、常用命令（5 秒上手）

```bash
# 日常开发
npm run dev              # 开发模式（Vite 端口 5500 + Electron 自动开）
npm run build            # 三进程编译到 dist/
npm run start            # 预览生产构建

# 质量门禁（提交前必跑）
npm run lint             # ESLint 严格模式（0 错误）
npm run typecheck        # tsc --noEmit
npm run test             # Vitest（1265 用例；不含覆盖率）
npm run verify           # 一键跑 lint+typecheck+test:cov+build（推荐；测试那步带覆盖率阈值，与 CI 同口径）

# 品牌资产生成（改色/改徽标后必跑，产物入库）
npm run build:tokens   # tokens/brand.json → generated-palette.css + design/palette.ts
npm run build:icons    # brand/*.svg → resources/icon.{png,ico}

# 打包
npm run package:win      # Windows NSIS 安装包
```

> ⚠️ 原有的 `build-dict` / `seed:demo` / `loop:*` 共 6 个脚本已于 2026-09-11 移除 —— 它们指向的 `scripts/` 目录已不在仓库（commit `2361f9e` 清除）。词典现在只能使用已提交的 `resources/dictionary.json`。

**提交顺序**：lint → typecheck → test → build（**全绿才可提交**）。

---

## 四、Sub-agent 协作约定 🔴核心必读

### 4.1 文件所有权（防冲突）

所有并行 Sub-agent 按下表划分文件所有权（**下表就是正本**，仓库内没有第二份机器可读的配置文件）：

| Agent | 可写 | 禁止 |
|-------|------|------|
| `renderer-agent` | `src/renderer/src/**` | `electron/**`、`shared/**` |
| `backend-agent` | `electron/**`、`shared/**` | `src/renderer/src/**` |
| `test-agent` | `tests/**` | `src/**`、`electron/**`（**只读**）|
| `infra-agent` | `*.config.*`、`.github/**`、`.claude/**` | 业务代码 |

**共享文件**（`package.json`、`AGENTS.md`、`CLAUDE.md`、根 `tsconfig.json`）由主编排器独占修改权。

### 4.2 并行开发协议

```
1. 主编排器读取 PRD → 拆解任务 + DAG 依赖分析
2. 文件归属映射（每个任务对应具体文件路径）
3. 按域分配 Agent → 并行执行
4. 共享文件修改前必须通知其他 Agent
5. 完成后集成 → 跑 verify 门禁
```

### 4.3 Sub-agent 模板

- **code-reviewer** — 7 维度审查（安全/性能/正确性/可维护性/测试/可访问性/文档）
  - 审查类目见本文 **§9.3 七维清单**（那就是正本）；提示词模板与反馈表达规范随任务下发，不落文件
- **test-writer** — Vitest 用例生成（红绿循环）

> 本章是**协作契约**，不是配置说明：仓库内没有任何 Agent 配置文件可读，规则的**可执行真值**在
> `eslint.config.js`、`tsconfig.json`、`vitest.config.ts`、`.github/workflows/ci.yml` 与下面 §5.3 的表里。
> `.claude/` 与 `.workbuddy/` 都在 `.gitignore` 里，换机后不会有、也不该去找。

---

## 五、自动化质量门禁 🔴核心必读

### 5.1 本地门禁（提交前）

| 门禁 | 命令 | 失败影响 |
|------|------|---------|
| ESLint 严格模式 | `npm run lint` | 阻塞 commit |
| TypeScript strict | `npm run typecheck` | 阻塞 commit（覆盖 `electron` / `src` / `scripts` / **`tests`**，覆盖面由 `tests/typecheck-coverage.test.ts` 钉住）|
| Vitest | `npm run test` | 阻塞 commit（只断言全绿，不算覆盖率） |
| 覆盖率 | `npm run test:cov` | **CI 上阻塞**（阈值 lines 83 / branches 80 / functions 75 / statements 83；2026-09-24 起 CI 的测试步骤就是这条）|
| Build 全通过 | `npm run build` | 阻塞 PR |

**一键验证**：
```bash
npm run verify
```

### 5.2 CI 门禁（GitHub Actions）

每次 push / PR 自动跑：
1. ESLint
2. TypeScript
3. Vitest + 覆盖率阈值（跑的是 `npm run test:cov`，阈值不过则 CI 红；`coverage/index.html` 与 `coverage-summary.json` 作为 artifact 保留 14 天）
4. 三进程 build

详见 [.github/workflows/ci.yml](.github/workflows/ci.yml)。

### 5.3 15 条硬性规则（违反即阻塞）

**下表就是这 15 条的正本**（仓库内没有第二份文本；过去这里指向过一个从未存在的规范正本文件，
2026-09-24 收掉 —— 一个"正本"不该有两种口径）：

| 类别 | 规则 |
|------|------|
| 🔴 安全 R1-R5 | 禁硬编码密钥 / 必参数化查询 / 错误响应不泄露 stack |
| 🟡 质量 R6-R10 | 覆盖率阈值见 `vitest.config.ts`（83/80/75/83）/ 文件 ≤ 500 行 / 圈复杂度 ≤ 15 / 目录 ≤ 4 层 / 0 lint 错误 |
| 🟢 规范 R11-R15 | Feature-First / 命名即文档 / Colocation / 配置外化 / Conventional Commits |

> ⚠️ 实测口径：`max-lines`、`complexity`、`max-depth` 在 `eslint.config.js` 中均为 **warn**（不阻塞）；仅 `max-params`、`prefer-const`、`eqeqeq`、`no-unused-vars` 为 error。故「违反即阻塞」仅对后四类成立。

---

## 六、领域专属规范

每个领域的规则**由谁强制**（刻意不另写一份规范文本 —— 本项目已在版本号、色值、IPC 通道上被"两份口径各自漂移"咬过三次）：

| 领域 | 可执行真值 | 重点 |
|------|-----------|------|
| 代码风格 | `eslint.config.js` + 根 `tsconfig.json` | TS/React/Electron 细节全在这两份配置里；`npm run lint` / `npm run typecheck` 是判定手段 |
| 安全 | `CLAUDE.md` §2 红线 + `SECURITY.md` | R1-R5 + IPC 边界；`contextIsolation: true` / `nodeIntegration: false` / 密钥不出主进程 |
| Git | `commitlint.config.js` + `.husky/` | Conventional Commits；pre-commit（lint+typecheck+test）与 commit-msg 两道钩子 2026-09-18 装妥 |

---

## 七、关键注意事项（Gotchas）

来自 [AGENTS.md 根级规则](d:/ai/claude%20code/%E5%BE%AE%E4%BF%A1%E8%AF%BB%E4%B9%A6/AGENTS.md)：

1. **端口 5500 硬编码** — 不要单独修改 `electron.vite.config.ts` 或 `electron/main.ts` 的端口（原 5176 因 Windows Hyper-V 保留端口范围 5175-5274 多次调整，现为 5500，两处必须保持一致）
2. **sql.js 是 WASM** — 默认内存运行，持久化必须显式 read/write
3. **preload path 解析** — `getPreloadPath()` 尝试多路径，改 build 输出需同步
4. **Windows-only 打包** — electron-builder 只配 NSIS，无 macOS/Linux
5. **中文 UI** — 所有用户字符串保持中文一致
6. **preload.ts 已解包** — `window.electronAPI.xxx()` 返回的是 data，不要再 `.data` 二次解包
7. **数据目录跟着应用名、不跟着安装目录** — 数据库/设置/日志全在 `app.getPath('userData')`（装机版 = `%APPDATA%\zhixing-reader`）。2026-09-21 起**只在非打包环境**把它换成 `zhixing-reader-dev`；装机版这个路径一个字都不能改（改了用户升级后看不到自己的数据）。`requestSingleInstanceLock()` 同样按 userData 上锁，所以换目录必须排在它之前。**2026-09-23 补：光排在锁前还不够** —— `logger` 与 `settings-service` 在**模块加载期**就绑定了 userData，而静态 import 全部早于 `main.ts` 本体执行，所以写在 main.ts 里的切换对它们等于没写（实测表现：开发版读不到自己的密钥、日志写进装机版目录）。切换逻辑因此提成 `electron/user-data.ts`，**它必须是 main.ts 最靠前的 import 之一**，由 `tests/dev-userdata-isolation.test.ts` 钉住顺序（sql.js 是整文件写回，共用目录会互盖数据）。

---

## 八、对话起手式

新对话开始时，按以下顺序加载上下文（避免一次性吞下全部）：

```typescript
// 1. 必须读：AGENTS.md（本文件）+ CLAUDE.md + .learnings/PROGRESS.md
// 2. 交接记录：.workbuddy/memory/ 下最新日期文件
// 3. 任务相关：CLAUDE.md §1 的「必须先读的文件」表
// 4. 任务代码：目标文件 + 上下游 ±200 行
// 5. 不读：node_modules、dist、release、resources
```

---

## 九、死代码治理经验（2026-07-21 循环工程沉淀）

### 9.1 死代码治理决策树

新增功能 / 修改按钮前必走：

```
死代码识别
├── 有微信读书 skill 能力支撑吗？
│   ├── 是 → 补齐真实功能（IPC + handler + preload + UI 全链路）
│   └── 否 → 砍掉按钮（直接删除，不要 disabled + tooltip 占位）
└── 是真实功能但 UX 差？
    └── 保留 + 优化（不在本循环处理）
```

**原则**：能砍则砍 / 能补则补 / 按钮要真。详见 `.learnings/LEARNINGS.md` LRN-20260721-010。

### 9.2 2026-07-21 死代码治理新增 IPC 通道

| 通道 | 用途 | 文件 |
|------|------|------|
| `WEREAD:FETCH_RECOMMENDATIONS` | 微信读书推荐好书（gateway 优先 + 衍生降级）| `weread-api.ts` `fetchRecommendations` |
| `SYSTEM:CLEAR_HISTORY` | 清理所有对话历史（runTransaction 包裹）| `database/schema.ts` `clearConversationsAndMessages` |
| `SYSTEM:RESET_DATABASE` | 重置数据库 17 张表 + `app.relaunch` | `database/schema.ts` `resetDatabase` |
| `ADMIN:CREATE_CUSTOM_PROMPT` | 新建自定义 AI 模板 | `services/prompt-storage.ts` |
| `ADMIN:UPDATE_CUSTOM_PROMPT` | 更新自定义 AI 模板 | 同上 |
| `ADMIN:DELETE_CUSTOM_PROMPT` | 删除自定义 AI 模板 | 同上 |
| `ADMIN:GET_CUSTOM_PROMPTS` | 拉取自定义 AI 模板列表 | 同上 |

### 9.3 死代码治理 7 维质量评分基准

verifier subagent 7 维审查标准（来自 dead-code-governance verify-report）：

| 维度 | 重点检查 |
|------|---------|
| 安全 | CSV 公式注入防御 / DB 重置多次确认 / `runTransaction` 包裹批量 DELETE / Modal `aria-modal` |
| 性能 | `runTransaction` 单事务批量 / `useMemo` 缓存 / Map 去重 / Promise.all 并行 |
| 正确性 | 幂等迁移 / `?.` 短路兼容旧数据 / 按钮 onClick 真实跳转 |
| 可维护性 | IPC 通道集中定义 / wrapper 转发解耦 / 类型从 shared/types 复用 |
| 测试 | 项目已有 Vitest（84 文件 / 1279 用例；纯逻辑 + 组件测试）。新增功能应补 `tests/*.test.ts`；提交前跑 `npm run verify`（含覆盖率阈值）|
| 可访问性 | Modal `role/aria-modal/aria-labelledby` + ESC + 焦点管理 |
| 文档 | 代码内注释 + 规范 commit message + **本文第十节一行批次记录**（早期设想的 spec/tasks/checklist/verify-report 四件套从未落地，实际沉淀位置见 §9.4）|

### 9.4 相关文件

经验与进度的**唯一**沉淀位置（都是本地文件、`.gitignore` 排除、不入库）：

- 踩坑与最佳实践：`.learnings/LEARNINGS.md` —— 本轮治理的结论在 LRN-20260721-006~010
- 路线图与待办：`.learnings/PROGRESS.md`（公开待办以 GitHub Issues 为准，见 §十）
- 会话交接：`.workbuddy/memory/` 下最新日期文件

---

## 十、变更记录

| 日期 | 变更 | 作者 |
|------|------|------|
| 2026-09-25（通道复扫） | **砍掉 25 条渲染层零调用的 IPC 通道，并当场抓到一条真断链 `admin:getPrompts`** —— ① **方法沿用 09-18 那次**：脚本列 preload 暴露面（158 个方法），逐个在渲染层找调用点（**必须先把空白全去掉再匹配**，因为 `highlight\n  .getById(id)` 这种跨行链式调用很常见，第一版没做这件事导致 3 个假阳性）。25 个候选逐个核实：通道常量只出现在「handler 注册 + preload」两处、渲染层零调用。② **抓到一条不是死链而是断链**：`admin:getPrompts` 有 preload 暴露、`PromptCenter.tsx` 在调，主进程**从来没注册过** ⇒ 后台「提示词中心」一进去就 reject（页面自己兜成"加载失败"）。`getAllAdminPrompts()` 函数一直是好的，只差一行 `handle()` —— 补上。这类洞 typecheck 与既有用例**一条都抓不到**（它们不看运行时），所以新增 `tests/ipc-channel-wiring.test.ts` 把它变成永久判据：**preload 里 `invoke()` 的每条通道必须有 `handle()`**；声明表里每条通道要么注册、要么是主进程推的事件（`.send(` 允许跨行，用 `\.send\((?:(?!;)[\s\S])*?IPC_CHANNELS\.` 收；`ipcRenderer.on(` 单独算订阅）；再一条反向钉住「注册/推送不许用声明表之外的常量」。③ **砍掉的 25 条**（通道 168 → **143**；preload 方法 158 → 133）：`highlight:search`、`cards:getById/create/createForExisting/update`、`articles:create/delete/getStats`、`vocabulary:getById/create`、`summaries:create/delete`、`conversations:getById/update/search`、`tokenUsage:getRecent`、`methodologies:getByBook/create/update/search`、`knowledgeCards:getById/getByBook/create/search`、`skill:generate`。判据是"零调用"，**不是**"看起来没用"：`cards:create` 背后那个 `cardsDb.create` 仍被划线新增路径内部调用（保留实现，只砍通道）。④ **自己踩的一刀**：批量删的时候 `IPC_CHANNELS.VOCABULARY.CREATE` 按**子串**匹配，把 `CREATE_FROM_LOOKUP` 的 handler 一起删了 —— 通道声明、preload、四个页面调用点都还在，只有主进程没人注册，而 typecheck / 全量测试**一条都不报**（这正好就是②要治的形状，本批先中招、当场被自己的守卫抓到）；补回 handler，并把「CREATE 是 CREATE_FROM_LOOKUP 的前缀」这一条写进反证用例。⑤ **对账**：**84 文件 / 1279 用例**，`npm run verify` 退 0（覆盖率 88.29 / 84.78 / 89.66 / 88.29 不变，新测试文件不进分母），eslint 0 error / 184 warning，preload 547 → 520 行。⑥ **下一步的量出来了**：这些通道背后有 12 个函数在产品侧已零调用（`cardsDb.createForExistingHighlights`、`articlesDb.delete/count/getTodayCount`、`vocabularyDb.getById`、`knowledgeCardsDb.getById/search`、`conversationDb.getById/update/search`、`tokenUsageDb.getRecent`、`chapterSummariesDb.deleteByBookId`、`bookSummariesDb.delete`），其中一部分仍被测试引用 ⇒ 删它们要连测试一起动，本批刻意没做（免得把"砍死代码"做成"砍绿灯"）。⑦ **真机对账**（CDP 连重启后的开发版，零花费）：`admin.getPrompts()` 交回 **22 条模板**，后台「提示词」页渲染出「管理 22 个提示词 · 已自定义 0 个」且不再报加载失败；被误删又补回的 `vocabulary:createFromLookup` 用已存在的词验到返回 `null`（不改数据）；四条已砍通道在暴露面上确实是 `undefined`，三条还在的（`book.getAll` / `card.getQueueStats` / `system.exportBackup`）是 function。⑧ **没验的**：❌ 其余 21 条被砍通道没有逐条点（判据是"渲染层零调用点"，脚本 + 类型双重核过）；❌ 装机版没跑过这批。 | AI Agent（接手） |
| 2026-09-25（备份要有下文） | **恢复完界面不再挂着旧数据 + 「多久没备份了」说得准**（上一批的收尾）—— ① **上一批自己留的尾巴**：备份改成整批替换后，恢复完只手动刷了设置页那三个 KPI 数字，别的页面里 `bookStore` / `reviewStore` / `profileStore` 挂着的还是**旧库**的数据 —— 用户看到的是"恢复了"，界面上却一本没变。逐个 store 发通知刷新等于再埋一处"忘了通知的那个页面显示旧数字"（这正是本项目反复治的病），所以改成**报完数就把界面重新加载一遍**（`window.location.reload()`，所有列表重新从库里读一次）。主进程不需要新通道，也不会有"半新半旧"的中间态。② **重载排在报数之后、并且留 1.5 秒**：一恢复就跳白屏等于什么都没告诉用户；`setTimeout(..., 1500)` 让 toast 先读完。③ **门禁由测试钉住**（`tests/data-io-import.test.ts` 4 条，happy-dom + `renderHook`，抓 `document.createElement('input')` 喂假 File）：取消确认 ⇒ `importBackup` 一次都没被调用、不重载；确认 ⇒ 报数含「3 本书」「1 条生成台账」，**报数当时不许重载**、1500ms 后才重载；主进程抛错 ⇒ 报错误且**不重载**（库里还是原样，页面不该闪）；旧格式备份 ⇒ 额外一句"当年没带走的类别带不回"。④ **顺手删掉一处因此变成死代码的东西**：`useDataIo(setKpiStats)` 的参数（重载之后页面自己会读，手动刷 KPI 那段成了 `return` 后面的不可达代码）—— 连 `safeNum` / `KpiStats` 两个随之没人用的 import 一起清，不给下一批留"看起来还在用"的假象。⑤ **备份提醒收成纯函数**（`src/shared/backup-reminder.ts`）：没导出过 ⇒「还没有导出过备份 · 数据只存在这台电脑上」；≥30 天 ⇒「上次备份是 2026/8/16 · 已经 40 天」；刚备份过 ⇒ `text: null`，面板那行**不出现**（不能永远亮着）。脏值（空串 / 解析不出 / `undefined`）一律按"没备份过"，时钟跳到过去（负数天）按"刚备份过"处理，不许报负数天。⑥ **界面上那一行**接在通知面板里，点它去 `/settings/data`；**不挂红点、不弹窗** —— 数据只在本地这件事值得说一句，不值得催。⑦ **对账**：**83 文件 / 1274 用例**，`npm run verify` 退 0（覆盖率 88.29 / 84.78 / 89.66 / 88.29，**被统计文件 54 → 56**：新加 `backup-reminder.ts` 与 `use-data-io.ts`；聚合 lines 从 89.27 降到 88.29 是因为 `use-data-io.ts` 只覆盖了导入那半条路（42%），**这是把没量的东西量出来的正常结果，不是回退**），eslint 0 error / 184 warning。⑧ **真机对账**（CDP，只点通知按钮与读写一个设置项，零 AI 花费）：三种情形逐一验到 —— 键不存在时面板出现「还没有导出过备份 · 数据只存在这台电脑上」；把 `lastDataExportAt` 设成 40 天前 ⇒ 面板变成「上次备份是 2026/8/16 · 已经 40 天」（不含 ISO 串）；设成今天 ⇒ 那一行整条消失；验完把该键置回空串（判定与"没备份过"等价）。**探针一条坑**：`document.body.click()` 关面板再 `btn.click()` 开面板，第一次有时序不稳（面板没开起来）⇒ 改成最多重试 6 次的循环，读的是渲染后的 `innerText`。⑨ **没验的**：❌ 「导入数据」那颗按钮的真实一次点击（含系统文件选择框）—— 门径与重载时机由组件测试钉住，真机只验了面板那一句；❌ 装机版没跑过这批。 | AI Agent（接手） |
| 2026-09-25（备份完整化） | **「完整备份」从此对得起这四个字：AI 生成物、生成台账、复习登记、对话历史都带得走、恢复得回来** —— ① **起点是上一批自己埋的雷**：`ai_generation_batches`（"这本书已处理 60/329 条划线"）落地后我去看备份实现，发现清单是 09-16 那次定的（books / highlights / cards / knowledgeCards / methodologies / vocabulary），新表**根本不在里面** —— 恢复后台账归零，界面按钮从「继续剩余」变成「重新蒸馏」，等于让同一批划线**再花一次钱**。② **量出三条真断链**（读代码 + 逐条对照，不是猜）：(a) 上面那条台账与 `chapter_summaries` / `book_summaries`（AI 摘要）都没进备份；(b) **备份里写了 `cards`，导入方从不读它** —— 只调 `createForExisting()` 按划线重建，于是 #88 之后"知识卡片 / 方法论加入复习队列"的登记恢复后全丢（那两类卡片 `highlight_id` 是 NULL，按划线重建根本重建不出来）；(c) 对话历史 `conversations` / `chat_messages` 完全没带走过，而它没有任何云端来源。③ **架构选择：把清单收成一份、把取数与恢复都挪进主进程**（`src/shared/backup.ts` 的 `BACKUP_TABLES` + `EXCLUDED_TABLES`，`electron/services/backup.ts` 的 `exportBackup/importBackup`），而不是再给渲染层加五条 IPC 去逐表取数。两条新通道 `system:exportBackup` / `system:importBackup`（**166 → 168**），渲染层只负责选文件、下载、报数。④ **恢复是"整批替换"而不是"缺什么补什么"**：旧写法逐条 `create` + 冲突跳过 ⇒ 同名行全被跳过，"已导入 0 本书"也会走成功分支；而且 FSRS 状态（`state/stability/difficulty/reps/lapses/due`）与台账这种"原样回来才有意义"的数据，用 create 重建必然丢。改成先按依赖**反序清空**、再按正序 `INSERT OR REPLACE`，整批在**一个事务**里（`runTransaction` 抛错即 `ROLLBACK`）⇒ 不会有"书回来了、划线没回来"的半套。界面上因此加了一次确认（这条路径会覆盖当前库）。⑤ **顺序不是随手排的**：`cards` 依赖 `highlights` / `knowledge_cards` / `methodologies`，`reviews` 依赖 `cards`，`chat_messages` 依赖 `conversations`，`vocabulary` 依赖 `articles`。测试拿库自己的 `PRAGMA foreign_key_list` 断言**清单是拓扑序**，并配反证（把 `cards` 提到 `highlights` 前面必须判红）。⑥ **带不走的东西写明理由**，而不是沉默漏：`token_usage`（调用用量日志，且会把调用明细写进明文备份文件）、`book_architecture`（链路 09-18 已砍，表是历史空壳）、设置与密钥（本来就不在库里，走 `settings.json` + `secure/*.enc`）。**库里新增一张表而它既不在清单也没写理由 ⇒ 测试判红**，这条守卫就是"清单悄悄漏东西"的解药。⑦ **列名与值同样收口**：插回时列名过上一批刚写的 `assertRealColumns`（同一张表同一套列只查一次库，几万行不重复跑 PRAGMA），值一律 `?` 占位符。⑧ **旧备份仍能用**：渲染层那套 v1.1 形状（没有 `tables` 分组、字段驼峰分组名）有专门的适配层，读得回来的部分照读，并**如实告诉用户带不回什么**（`legacy: true` ⇒ 提示"当年的清单里没有复习记录、对话、摘要与台账"）；既没形状也没标记的文件一律拒收（不是本应用的备份 / 版本比程序还新 / 某张表不是行数组 / 清单外多一张表）。⑨ **守卫**：`tests/backup-roundtrip.test.ts` 9 条 —— 真实 sql.js 库上「种 15 类数据 → 导出 → 清空 → 导入」逐类比对；专设两条盯本次修的洞（台账 `getProcessedIds` 恢复后仍是 `['h1']`、三来源复习卡片各自回来且 `reps` 不变）；一条坏列名混在整批里 ⇒ **整笔回滚**（库里保持导入前原样，不是"插了一半"）；拓扑序与覆盖完整性两条各配反证；`describeImportedCounts` 钉住报数口径。⑩ **对账**：**81 文件 / 1265 用例**，`npm run verify` 退 0（覆盖率 89.23 / 84.76 / 89.57 / 89.23，**被统计文件 52 → 54** —— 新加的 `src/shared/backup.ts` 与 `electron/services/backup.ts` 有测试就进清单），eslint 0 error / 184 warning（新增文件没带老债）。⑪ **真机对账**（CDP 连重启后的开发版，零 AI 花费）：先给开发库留一份文件还原点，再跑完整往返 —— `exportBackup()` 如实报出 `books 92 / highlights 934 / cards 934 / knowledge_cards 90 / methodologies 11 / vocabulary 12 / articles 80 / daily_stats 12 / reviews 10 / memories 2`（会话与摘要类这台机器上确实是 0，如实是 0）；把同一份 payload 导回去，**逐类计数与界面指纹前后完全一致**（含队列第一张卡的 `reps`），日志落 `Backup imported {"version":"1.2","legacy":false,...}`，库文件按原字节数重新落盘。跑完删掉还原点（里面是用户数据，不留副本）。⑫ **没验的**：❌ 界面上「导出/导入数据」两颗按钮的真实点击（含导入那次确认框）—— 只验到 IPC 与主进程落库；❌ 一台机器导出的备份拿到**另一台机器**恢复（同一台只有一个 sql.js 实例，验不到跨机的 `secure/*.enc` 与 DPAPI 差异 —— 备份本来就不含密钥，这条只说明"设置不会跟着走"这件事没在真机上确认过）；❌ 装机版没跑过这批。 | AI Agent（接手） |
| 2026-09-25（行形状收成一份） | **跨进程交回的就是库里那一行，声明别再写映射之后的样子**（渲染层 `as unknown as` 23 → **3** 处、`as unknown[]` 22 → **0**）—— ① **病根是一句话**：`renderer.d.ts` 把 `book.getAll` / `highlight.getByBook` 这些通道声明成 `Book[]` / `Highlight[]`，而那些是 `utils/db-mapper` **映射之后**的形状；主进程实际是 `SELECT *` + `rowsToObjects`，交回的是下划线列名。声明撒谎的代价不是报错，是**每个消费方自己补一层硬转**：全渲染层 22 处 `as unknown[]` + 若干 `as unknown as`。② **改法是让声明说实话**（`Array<Record<string, unknown>>` / 单行 `Record<string, unknown> \| undefined`），硬转随之失去存在理由；`create` / `update` 那些声明成 `Promise<Book>` 而 handler 其实返回 `void` / `boolean` / `{ id }` 的也一并改对。③ **改完立刻抓到一处真 bug 的形状**：`sync-bookshelf` 把 `book.getById` 的结果 `as Book \| null` 再用 —— 它只用到 `existing.id`（而那个 id 就是查询用的 `wb.bookId`），改成直接用 `wb.bookId`，少一次撒谎也少一次"读到一个不存在的字段"。④ **`fsrs:previewReviewRatings` 的"两种形状都认"是死分支**：handler 里 `const hasSnake = 'highlight_id' in card`，而渲染层唯一调用方（复习页预览）传的是 `card.getDueWithContent` 交回的驼峰 `DueReviewCardView` —— 蛇形那一支从未走到。改成通道收 `ReviewCardFields`（`src/shared/review-sources.ts`，与引擎 `Card` 同字段、已由 `tests/review-queue-contract.test.ts` 对账），主进程 `cardFromFields()` **逐字段赋值**而不是整体硬转：将来两边漂一个字段就编译不过。⑤ **复习记录导出的列名钉进类型**：`REVIEW_CSV_COLUMNS` 的 `key` 从 `string` 改成 `keyof ReviewRow`，`reviewsDb.getRecent()` 也从 `Record<string, unknown>[]` 改成显式映射出 `ReviewRow[]`。这一条属于 2026-09-20 那个 bug 的根治 —— 当时六列里五列常年空白，因为读的是根本不存在的 `cardId` / `reviewedAt`；列名写错今后是**编译错误**，不再是导出一列空白。⑥ **管理后台知识库页不再自带行类型**：三份页面内联的 `Book` / `Highlight` / `KnowledgeCard` 删掉，改走 `mapBooks` / `mapHighlights` / `mapKnowledgeCards`；`highlight_count` 是那条查询自己 JOIN 出来的（books 表没这一列），所以单独收进 `highlightCounts` 映射而不塞进行类型。⑦ **守卫**：`tests/db-row-types.test.ts` 26 → 31 条（新 describe：声明里不许再出现 `Promise<Book[]>` / `Promise<Highlight>`，正向配一条"至少 8 个通道声明成了原始行"，反证用收口前原文并确认 `BookRow` / `BookSummary` 这类合法名字不被误伤；另加一条扫全渲染层不许再出现 ` as unknown[] `）、`tests/fsrs-engine.test.ts` 47 → 50 条（`cardFromFields` 与直接调用**预览结果完全一致**、两个空来源补 `null` 而不是 `undefined`、逐字段真搬的反证；通道源码扫描钉住 `'\w+' in card` 不许回来 + 反证）。⑧ **对账**：**80 文件 / 1256 用例**，`npm run verify` 退 0（覆盖率 88.94 / 84.44 / 89.2 / 88.94，被统计文件 52 不变），eslint 0 error / 188 warning。⑨ **真机对账**（CDP 连重启后的开发版，零 AI 花费）：复习预览用队列里真实的卡跑出 4 档 `[1,2,3,4]` 且每档都带人话间隔（`rating 1 → "1 分钟"`）；`highlight.getById` 找不到时确实是 `undefined`、取回的一行带的是 `chapter_title`（下划线列名）；后台知识库页 92 本书渲染出「思考，快与慢（第二版） / [美]丹尼尔·卡尼曼 / 140 条笔记」，展开一本有 8 张卡的书后「划线 / 笔记」与「知识卡片 (8」两个小节都在；`review.getRecent(3)` 交回的键集合恰好等于 `ReviewRow` 那六列、没有驼峰残留。⑩ **踩到的两条**：(a) 用脚本批量删 `as unknown[]` 时，正则把**调用括号**一起吃掉了（`mapBooks(booksRaw as unknown[])` → `mapBooksbooksRaw`），16 处被破坏后用括号平衡检查补回 —— 教训是**机械搬运脚本必须只对边界配对（typecheck）负责，别只数替换条数**；(b) 那两个页面（KnowledgeCards / Methodologies）工作树里是 CRLF（早前 python 脚本写的），`git diff` 会显示整文件改动，看清真实改动要用 `git diff --ignore-cr-at-eol`。⑪ **没验的**：❌ 界面上「编辑划线」保存按钮与复习预览的真实点击（只验到 IPC 与页面渲染文字）；❌ 复习记录 CSV 真导一份出来看（那条路径会弹保存框，自动化不碰）；❌ 装机版没跑过这批。 | AI Agent（接手） |
| 2026-09-25（SQL 列名白名单） | **拼进 SQL 的列名必须先证明是那张表的列** —— ① **起点是读代码时看到的一句 `setClauses`**：`books` / `highlights` / `methodologies` / `knowledge_cards` 四个 `update(id, record)` 都写 `Object.keys(record).map(k => `${k} = ?`)`，而 record 来自渲染层（`BOOKS.UPDATE` / `HIGHLIGHTS.UPDATE` / `METHODOLOGIES.UPDATE` / `KNOWLEDGE_CARDS.UPDATE` 四条通道整包下发）。值确实走了 `?` 占位符，**但占位符管的是值不是列名**：`{ 'content = 1, note': 'x' }` 会拼成 `SET content = 1, note = ?` —— 键名里带逗号就改写了整条子句，等价于让界面决定 SQL 形状。项目 R2 写的是"必参数化查询"，这一条以前只做到了一半。② **判据只有一个来源：库自己**（`PRAGMA table_info(table)`）。**没有另写一份列清单** —— `conversations.ts` 里那份手写的 `UPDATABLE_COLUMNS` 就是反面教材：加一列要记得改清单，忘了就是"这个字段存不进去"，而清单本身永远不会报错。③ **不合法就在建 SQL 之前抛**，所以不存在"合法的那半条写进去了、非法的那半条报错"（同一条 record 里混着两种键时实测整条不发，用例钉住）。④ **顺带把空 record 的崩溃补掉**：四个构造器原来传 `{}` 会拼出 `SET , updated_at = ...` 直接语法错，现在 `length === 0` 早退（与 `conversations` 既有写法一致）。⑤ **knowledge-cards 的驼峰映射不能弄断**：它先 `fieldMap[k] ?? k` 再拼，守卫接在映射**之后**（`{ reviewCount: 3 }` 仍写进 `review_count`，有用例）。⑥ **同类横扫**：`electron` 全量扫"把变量当列名拼进 SQL"的写法，抓到四处已修；**带理由豁免**四类 —— `cards.ts`（`CARD_UPDATABLE_COLUMNS` 常量清单）、`conversations.ts`（同上，且它只接受自己那份业务白名单）、`deleted-archive.ts`（自带 `assertSafeIdentifier`，列名来自库里读出的行）、`base-repository.ts`（键名由子类 `mapToRow` 逐字段赋值，不接受调用方的键）。`memories.ts` 的 `OR content LIKE ?` 只拼占位符个数，不拼标识符，确认不在其列。⑦ **守卫**：新增 `tests/sql-column-whitelist.test.ts` 20 条 —— 四张表各三条（合法列写入 / 键名带 SQL 片段被拒且原值不动 / 非法列混在里面整条不发）+ 驼峰映射 + `id` 仍被排除 + 守卫函数本身三条（含"表名拼错时判红而不是静默放行"）+ 源码扫描两条**带反证**（把收口前的两种写法喂进判据必须命中；值占位符 `IN (${placeholders})` 不许命中，否则这条守卫天天误报）。**先跑未接守卫的版本实测 5 条判红**（四条注入用例 + 一条扫描），接上守卫后 20 条全绿。⑧ **对账**：**80 文件 / 1246 用例**，`npm run verify` 退 0（覆盖率 88.89 / 84.42 / 89.17 / 88.89，被统计文件 52 —— `electron/database/*.ts` 那条 glob 自动收了新库文件），eslint 0 error / 188 warning。⑨ **真机对账**（CDP 连重启后的开发版，主进程改动必须重启才生效；零 AI 花费）：用应用自己的 IPC 造一条临时划线 → 合法列 `{ content }` 写入生效 → 恶意键名 `{ 'content = 1, note': '改写' }` 返回错误 `highlights 表没有这些列，拒绝拼进 SQL：content = 1, note`，且 `content` / `note` 一字未动 → 临时划线按 `system.archiveDelete` 清掉，探针不留痕。⑩ **没验的**：❌ 界面上「编辑划线」那颗保存按钮的真实点击（只验到 IPC 层）；❌ `articles` / `vocabulary` / `daily_stats` 三张表没有同类拼接（量过，它们的 update 是写死列名的 SQL），所以本轮没为它们加守卫；❌ 装机版没跑过这批（出包通路仍卡着，见 #1）。 | AI Agent（接手） |
| 2026-09-25（会话行与死兜底） | **conversations / chat_messages 有了一行一形，`daily_stats` / `reviews` 的"两种写法都认"清干净** —— ① **起点是上一批自己留下的那条**（廿七续末尾写的"值得单开一条"）。审计做法是先问"谁在生产这些行"：`conversationDb.getAll/getMessages`、`admin.getAdminSessions`、`dailyStatsDb.getToday/getRange` **全是 `SELECT *`** ⇒ 运行时只能是下划线列名。② **量到的谎**：`src/shared/types.ts` 的 `Conversation` / `ChatMessage` 写的是驼峰（`bookId` / `createdAt` / `conversationId`），而 `renderer.d.ts` 恰好就用它当通道返回类型 —— 那份声明与运行时一个字段都对不上；今天没出错数字，只因为**每个消费者都把类型 cast 掉了**（chatStore 立刻 `as unknown as Record<string, unknown>[]`、后台页再自己写一份下划线的 `Session`）。同一份会话行因此有三份声明。③ **收口**：`utils/db-mapper.ts` 里 `ConversationRow` / `ChatMessageRow`（= IPC 交出去的真实列名，`renderer.d.ts` 用它）+ `ConversationView` / `ChatMessageView`（= 界面用的驼峰形状，JSON 文本列在这一处解析：`sources` / `tools_used` / `mastery_assessment`，0/1 走 `safeBool`）。chatStore 的 `Session` + `mapSession` + `RawMessage` + `mapMessage` + `safeParse` 全删（约 60 行），后台 `SessionHistory` 的两份本地类型也删。④ **顺手清掉的死兜底**：`normalizeDailyStatRow` 里 `raw.reading_time ?? raw.readingTime ?? raw.readingTimeSeconds` 三支、`summarizeReviews` 里 `r.review_time ?? r.reviewTime ?? r.created_at`（**reviews 表没有 created_at**）、`Stats.tsx` / `DailyLearning.tsx` 各一处 —— 后两种拼法没有任何生产方，且 `DailyLearning` 那句注释还把原因写错了（说"renderer.d.ts 把 DailyStats 声明成驼峰"，其实 `DailyStatsRow` 已是下划线）。函数签名改成收真实行类型，拼法问题从此由类型挡。⑤ **一个必须说清的区别**（差点一刀切错）：`.readingTime` 这类驼峰**不是哪里都禁** —— `ActivityDay.readingTime` 是我们自己算出来的合法字段，所以扫描判据只钉"确实没有生产方的三种拼法"，另加一条"归一化函数不许再出现 `raw.x ?? raw.y` 这种一行读多种拼法"。⑥ **自己踩到的两处**：(a) 我在解释注释里写了被禁的 token，源码扫描当场判红（本项目已知坑，又踩一次）；(b) `tests/profile-stats.test.ts` 有一条用例断的正是"camelCase 形状也接得住" —— **把缺陷写成了期望**，改成断"驼峰读不到就是 0，不猜"，生产方不存在这件事由上一条用例（下划线形状四字段不丢）钉住。⑦ **守卫**：`tests/db-row-types.test.ts` 22 → 26 条（两张新表进双向对账；`ConversationView.bookTitle` 走 `aliases: ['book_title']`，它是 JOIN 出来的列不是 conversations 的列；具名类型必须显式登记是字符串联合还是对象|null，不靠名字猜）+ `tests/no-fake-controls.test.ts` 一段新扫描含反证（把收口前四段原文喂进去必须命中）。⑧ **对账**：**79 文件 / 1226 用例**，`npm run verify` 退 0（覆盖率 88.54 / 84.52 / 88.93 / 88.54，被统计文件 51），eslint 0 error / 188 warning。⑨ **真机对账**（CDP）：开发库里 `conversations` / `chat_messages` **实测 0 条**（早先被清过），所以直接读等于没验 ⇒ 用应用自己的 IPC 造一条临时会话（两条消息，一条带 `sources`），核到原始行确实是 `id,title,book_id,created_at,updated_at,message_count,history_summary`、`sources` 是 JSON 字符串、`liked` 是数字；过映射器后 `messageCount` 2、`sources` 成长度 1 的数组、`liked` 是布尔；**删完库与 store 都回到 0 条**。档案页读到 `59 分钟 / 9 天有读 · 日均 7 分钟`（`daily_stats` 12 行真实数据）。⑩ **没验的**：❌ 点赞/收藏的真实点击（只验到解析后的字段类型）；❌ 有真实多轮历史会话下的会话切换（临时会话只有两条消息）；❌ 装机版没跑过这批。 | AI Agent（接手） |
| 2026-09-25（生词与文章行映射） | **`vocabulary` / `articles` 两张表补上行映射层（Issue #3 做完）** —— ① **起点是 09-22 那批明确留下的接缝**：书 / 划线 / 卡片 / 知识卡片 / 方法论五套行类型收口时，生词与文章被点名"仍各自硬转"，本机开发库里 `as unknown as` 剩 32 处，其中生词与文章这条占 9 处。② **真正的病不是 cast 多，是同一个行有三份声明**：`pages/vocabulary/model.ts` 的 `VocabularyItem` 写 `is_mastered: number`，`pages/daily-learning/constants.ts` 的 `Vocabulary` 写 `is_mastered: boolean`，文章另有 `Article` 配一个页面自己的 `normalizeArticle` —— 于是生词本里比较 `v.is_mastered === 1`、每日学习里当布尔用，两边都对不上 `vocabulary.is_mastered INTEGER`。③ **收口成一份**（`utils/db-mapper.ts` 的 `VocabularyRow` / `ArticleRow` + `mapVocabulary/mapArticle` + 新的 `safeBool`）：这两个行类型**故意不带 `[key: string]: unknown` 索引签名** —— 与 BookRow 那套不同，页面读一个库里没有的列会直接编译不过，这正是这一层该干的事。④ **字段名保留库里的下划线**（`meaning_zh` / `title_en` / `is_read`）而不是改驼峰：这两张表的列名已经是界面契约（六个文件在读），改驼峰只为好看要动一整片读取点，收益是零；这一层的价值在类型不再撒谎。⑤ **顺手删掉两个没人读的声明**：`ArticleRow` 一度把 `vocabulary_json` 改名成 `vocabulary: string[]` 收进来 —— 全仓库除了 INSERT 没有任何读方，等于给"将来也许用"编一个字段，删了。⑥ **`Profile` 那处死分支一并清掉**：`vocabStats as unknown as Record<string, unknown>` 后面写着 `total ?? totalCount ?? count` 三个候选，而 handler 返回的就是 `{total, mastered, dueToday}` 且 `renderer.d.ts` 早就这么声明 —— 类型在撒谎时才会需要这种三连兜底。⑦ **守卫扩到这两张表**：`tests/db-row-types.test.ts` 15 → 22 条，正向（拿真库 `PRAGMA table_info` 的列喂给映射器，要求声明了的每个字段都造得出来、类型对得上）+ 反向（映射器不许读库里不存在的列）+ **反证**（把 `VocabularyItem` / `as unknown as Article[]` 这些收口前的原文喂进扫描判据，必须命中，证明"旧类型不许回来"那条不是空转）+ 一条断 `0/1 → boolean` 与 `ef_factor` 缺列回退 2.5（库里 DEFAULT 2.5，两份口径会算出不同调度）。⑧ **对账**：**79 文件 / 1218 用例**（+7），`npm run verify` 退 0（覆盖率 88.52 / 84.63 / 88.93 / 88.52，被统计文件 51 不变），eslint 0 error / **188 warning**（比上一批少 1 条，就是删掉那个三连兜底带的）。渲染层 `as unknown as` **32 → 23 处**：剩下的是 chatStore(4) / admin 后台(5) / profileStore(2) / DailyLearning 的 `daily_stats` 与 `conversations`(2) / 渲染层自带的测试文件(4) / `import-weread-content`(1，**故意**：它要区分"章节名为 NULL"与"未知章节"，过映射器会被默认值抹掉) / reviewStore + use-data-io + Stats + echarts 主题(各 1)。⑨ **真机对账**（CDP 连跑着的开发版，只读不花钱）：生词本照常渲染出库里那 12 个词（`leading` / `fame` 与其中文义项都在）；经 `#/daily-learning?article=<id>` 深链进文章视图，标题、难度标签「四级」（`difficulty='cet4'` → `DIFFICULTY_LABELS`）、未读与收藏按钮全在位；顺带量到 IPC 原始行里 `is_read` 确实是数字 `0` —— 布尔化发生在映射器，这正是这层的职责边界。⑩ **踩到的一条工具坑**：探针里 `location.hash` 改完要等 1.5~2 秒才拿到渲染后的 `innerText`，第一次跑没延时就会读到上一页的文字误判"没渲染"。⑪ **没验的**：❌ 生词「复习评分」那条交互没真点过（改的是行类型与布尔口径，评分链路有既有用例覆盖）；❌ `VocabularyDrawer` / `ExportModal` 里那些字段在界面上的观感（只验到列表渲染与文字在位，不做视觉验证）；❌ 装机版没跑过这批。**剩下的同类接缝**：`daily_stats`（`renderer.d.ts` 声明驼峰、运行时是下划线，全项目靠"两种写法都认"绕过）与 `conversations` 两套 —— 那是下一个域，本批刻意不混。 | AI Agent（接手） |
| 2026-09-25（分批续跑） | **AI 生成从"只能覆盖开头一批"变成"点几下生成完整本书"**（#87 的下半场，方向由维护者选：三条候选里挑了花费不变的那条）—— ① **先量清楚为什么不能用现成字段当进度**：`knowledge_cards.source_highlight_id` 每张卡只标 1 条、`methodologies.source_highlight_ids` 每条最多标 3 条（提示词里就 `slice(0, 3)`），一批 20 条划线常常只落下 5 个标记 —— 拿它当"处理过多少"会把大量已处理的算成没处理，下一批再花钱喂一遍。所以**新增第 17 张表 `ai_generation_batches`**：一行记一整批的 id 集合（JSON 数组），外键指向 books 且 `ON DELETE CASCADE`，`resetDatabase` 的清单同步加上。② **上限、计划、按钮语义全收在 `src/shared/ai-coverage.ts` 一份**：`planAiCoverage(task, total, alreadyProcessed)` / `pickUnprocessed` / `describeBookCoverage` / `resolveGenerateAction` / `generateButtonLabel`；`ai-service.ts` 里那两处硬编码（`slice(0, 50)` 与 `DISTILL_MAX_HIGHLIGHTS`）已没了，`tests/ai-coverage.test.ts` 带反证把它们钉住。③ **三种按钮语义分开写**：`start`（没成品 → 从头生成）/ `continue`（有成品、有可追溯进度、还剩一批 → 只补剩下那批）/ `replace`（有成品但进度无从追溯，或整本已处理过 → 清空重来）。把"有成品没进度"判成 continue 会静默重复生成一批卡片（重复花钱 + 内容翻倍），判成 replace 才是安全的 —— 台账上线前生成的那 90 张卡就是这种情况，第一次点会走确认框而不是追加。④ **成品清空 ⇒ 进度自动作废**（读侧 `describeBookCoverage` 与写侧生成前各调一次同一规则）：卡片全删了还让台账声称"这 60 条处理过了"，用户就永远补不回那批 —— 这是本项目反复治的"数据说自己处理过了，其实没有"。⑤ **整本处理过时一次 AI 都不发**（`nothingNew: true`，界面说"没有新的内容"而不是"完成"）；台账只在**卡片落库成功之后**才记，半途失败的那批下次点还能补上。⑥ **通道 164 → 166**（`knowledgeCards:coverage` / `methodologies:coverage`），各自只有一个消费者（蒸馏中心 / 方法论页），按书分组一次 SQL 取全，不逐本查。⑦ **界面**：蒸馏中心每本书显示「已有 N 张卡片 · 已处理 60/329 条划线」（没有可追溯进度时不摆那串 0，会被读成"这本书白做了"），按钮 `开始蒸馏 / 继续剩余 269 条 / 重新蒸馏`；方法论页提取面板与按书分组走同一条 `resolveGenerateAction`。⑧ **对账**：**79 文件 / 1211 用例**，`npm run verify` 退 0（覆盖率 88.52 / 84.63 / 88.93 / 88.52，被统计文件 51 —— `electron/database/*.ts` 那条 glob 自动把新库文件收进来了），eslint 0 error / 189 warning。新增 `tests/ai-batches.test.ts` 10 条（含"坏 JSON 按空批处理""删书级联带走台账""没记过台账时读侧就是 0"这条反证）与 `tests/ai-coverage-channel.test.ts` 10 条（注册**真实的** `registerKnowledgeHandlers`，喂的是假 `extractMethodologies`，全程零网络零花费）。⑨ **真机对账**（CDP 连跑着的开发版；探针只读不点任何花钱的按钮）：重启后新表在真实开发库建好（17 张表 / 台账 0 行 / 指向不存在书的台账 0 条），两个 coverage 通道对 9 本有划线的书返回 329 / 196 / 160 / 140 / 68 / 28 / 8 / 4 / 1，与直接查库逐本一致；蒸馏中心 92 个书格里实际渲染出的按钮只有「开始蒸馏」与「重新蒸馏」两类（有卡片但进度无从追溯的那 9 本走「重新蒸馏」+ 确认框），卡片行显示为 `已有 39 张卡片` 而**不带** `已处理 0/329` 那串 0 —— 第一版摆了 0，实测读起来像"这本书白做了"，改成没有可追溯进度就不说。⑩ **一条探针踩的坑值得记**：这个应用是 **hash 路由**（`#/knowledge-cards`），探针用 `history.pushState('/knowledge-cards')` 导航会得到一个空白页 —— 改成 `location.hash = '#/...'` 才真渲染出内容；`/json` 列出的 page target 里第一个是 DevTools 窗口（`window.electronAPI` 是 undefined），要按 URL 挑真正那个。⑪ **没验的**：❌ 一次真实的续跑点击（要点就会花钱，未批不做）—— 所以"第二批喂进去真的产出卡片吗"这句只有单元与通道级证据；❌ 台账与 `cards` 三来源那套的联动（知识卡片被移出队列/删除时台账不动，这是有意的：进度说的是"喂过"，不是"还剩几张卡"）；❌ 装机版没跑过这批（出包通路仍卡着，见 #1）。 | AI Agent（接手） |
| 2026-09-25（AI 生成覆盖数） | **「蒸馏完成」从此说清覆盖了整本书的多少**（#87 的第一半）—— ① **起点是读代码读出来的，不是用户报的**：`distillKnowledgeCards` 把输入硬截到 60 条划线、`extractMethodologies` 硬截到 50 条，而界面上只有一句「知识卡片蒸馏完成」。一本 312 条划线的书，点一次生成实际只处理了 60 条，看起来却像整本书都变成卡片了。② **上限本身不是缺陷**：截断是为了单次请求的上下文预算与超时（笔记越多回得越慢），真正的问题是**不说不然**。所以这一批只做"如实"，没动"覆盖全书"—— 后者是把每次点击的 AI 花费乘上约 5 倍，按花钱边界要先报数再问人。③ **一份真值**：新增 `src/shared/ai-coverage.ts`（`AI_INPUT_LIMITS` 60 / 50 + `planAiCoverage` + `takeWithinLimit` + `coverageNotice`）；`ai-service.ts` 两处硬编码改走 `takeWithinLimit`，服务层与 IPC 用同一个 `planAiCoverage` 派生覆盖数 —— 截断的条数与报出的条数是同一次计算的结果，漂不了。④ **措辞里刻意不写"前/后"**：划线在库里按 `created_at DESC` 排，被取走的 60 条是"最近的"而不是"书的前 60 条"，写"前"会给出错误的心理模型（这条写进了模块注释）。⑤ 两个生成通道的返回值从裸数组改成 `{ items, coverage }`（`cards` / `methodologies` + `coverage: CoveragePlan`），渲染层两条 toast 文案接上覆盖数，完整覆盖时不啰嗦；蒸馏进度事件的 `stage:'done'` 消息同样带上。⑥ **对账**：**77 文件 / 1171 用例**，`npm run verify` 退 0（覆盖率 88.2 / 84.29 / 88.41 / 88.2，被统计文件 49 → 50 —— 新模块进清单，实测 100%），eslint 0 error / 188 warning（新增 2 条是 `complexity` 与 `max-lines-per-function` 的存量告警，本项目不阻塞）。新守卫 `tests/ai-coverage.test.ts` 12 条，含反证：把改动前的 `slice(0, 50)` 与 `DISTILL_MAX_HIGHLIGHTS` 两种旧写法喂进扫描判据，必须命中（判据不是空转）；`knowledge-card-service.test.ts` 补 1 条"100 条划线 ⇒ covered 60 / skipped 40 / partial"，断的是**返回给界面的那个数**。⑦ **自己的测试抓到自己一处算术**：`planAiCoverage` 第一版对负数输入做了 clamp，但 `skipped` 漏 clamp（会报「-5 条未处理」）—— 判下来 `highlights.length` 不可能是负数，于是**把 clamp 整个删掉而不是补一行**，测试改成断 0 条的情形。⑧ **没验的**：❌ 那条 toast 的真实观感没点过（本轮零 AI 调用、零界面点击）；❌ 覆盖数在真实一次生成里对不对 —— 要用户授权跑一次才有数；❌ 装机版没跑过这批。**剩下的半边**：「让一次生成真覆盖整本书」等维护者对花费表态再做（候选：分批续跑 / 提高单次上限 / 保持现状只说清楚）。 | AI Agent（接手） |
| 2026-09-25（复习队列三来源） | **AI 生成的东西从此能进复习队列**（#88）—— ① **起点是功能缺口不是缺陷**：复习队列只认划线，知识卡片与方法论生成完就只能读，间隔重复这条主线对它们完全没接上。② **schema 选型走的是"外键保得住"的那条路**：给 `cards` 加 `highlight_id / knowledge_card_id / methodology_id` 三个可空外键列 + `CHECK`「有且只有一个来源」，而不是多态的 `source_id` + `source_kind` —— 后者只能有一个真外键，另两类来源删掉之后复习卡片会留在库里，且 `kind` 与 `id` 各说各话（本项目在数据完整性上已经栽过一次"级联静默失效"，见上一行）。③ **迁移是整表重建**：`cardsTableDdl(tableName)` 一份 DDL 同时服务"新建库"与"重建老库"（建新表 → 按两份列名交集拷贝 → drop 旧表 → rename），`PRAGMA foreign_keys` 关闭包在 `try` 里、`finally` 打开。④ **让应用碰用户数据之前先只读跑一遍**：拿真实开发库（92 书 / 934 划线 / 934 卡 / 10 复习）在 node 里把迁移走完整，逐条对账迁移前后卡片数与字段一字不差、孤儿 0，才接进 `initializeSchema`。⑤ `electron/database/cards.ts` 重写：`CARD_COLUMNS` 一份列名清单（`INSERT`/`UPDATE` 的值从同一份派生，不再两处各写一遍）、`CARD_WITH_BOOK_SQL` 用 `COALESCE(h.book_id, kc.book_id, m.book_id)` 取书、`getDueCards` 不再按划线 JOIN 过滤（这是知识卡片类进不了队列的直接原因）。⑥ **正/背面文案只有一份**：`src/shared/review-sources.ts` 按来源类型给 `label/front/back/detail/sourceLine`，划线问"还记不记得这句"、知识卡片摆步骤与方法论名、方法论问"哪一段用到它"；渲染层与复习页都从这里取，改措辞测试立刻红。⑦ **通道 161 → 164**：`cards:enroll`（单条与批量同一条，返回 `{created, skipped, actionable}`）、`cards:unenroll`、`cards:enrolledSources`；`requireKind()` 白名单 + `ids` 数组校验都在写之前抛，校验失败的用例断言"库里什么都没变"。⑧ **界面按同一条路走**：`use-review-enrollment(kind)` 一个 hook 管两类页面（状态每次从 `card.enrolledSources()` 回读，不在本地攒一份副本），列表页批量按钮 + 详情页每条按钮（`aria-pressed`）。⑨ **`createCard` 的入参是联合类型** `ReviewSourceRef | string`：改成只收对象要让 30 处测试各包一层，规范化只写一处更便宜。⑩ **顺带把删除现场 generalized**：`deleted-archive.ts` 从硬编码划线改成 `ARCHIVE_SPECS` 表（四类各自的主表、删除方法、卡片列），删知识卡片/方法论现在也连带走它的复习卡片与复习记录，撤销能还原 FSRS 进度（`tests/undo-delete.test.ts` 新增 1 条）。⑪ **一条已有测试把这次救回来了**：中期把 `highlight_id` 的 JOIN 去掉后 `book_id` 就没了 —— `tests/card-book-id.test.ts`（上一批攒的）立刻判红；这正是本项目警惕的反面情形，"测试绿"不等于对，但这次是"测试红"证明它盯得住。⑫ **对账**：测试 **74 文件 / 1138 用例 → 76 文件 / 1158 用例**，`npm run verify` 退 0（覆盖率 88.16 / 84.23 / 88.34 / 88.16，被统计文件 48 → 49）。新增 `tests/review-queue-sources.test.ts` 14 条（`LEGACY_CARDS_DDL` 逐字取自 `git show v1.3.4:electron/database/schema.ts`，钉住"迁移认的老库形状是历史真实而非我想象的"）与 `tests/review-enroll-channel.test.ts` 5 条（注册**真实的** `registerBookHandlers` 再调 handler）。⑬ **真机对账**（CDP 连跑着的开发版、走真实 IPC）：入队 → `{created:1}`；同一张再入队 → `{created:0, skipped:1}`，没有第二张；`enrolledSources` 回读到那个 id；队列新卡池 924 → 925；移出 → `{removed:true}`、名单回空、卡片本身仍在。⑭ **没验的**：❌ 界面上那颗按钮的真实点击没点过（只验到 IPC 往返与渲染层数据）；❌ 三类来源混在一起跑一轮真实复习的观感（队列计数对过，评分链路没为它们单独走一遍）；❌ 装机版没跑过这一批（出包通路仍卡着，见 #1）。 | AI Agent（接手） |
| 2026-09-25（外键复位修复） | **删完东西库里还有孤儿行 —— 根因不在 schema，在 sql.js 的 `export()`** —— ① **起点是一条实测，不是一条猜测**：上一批给「删除可撤销」做真机核对时（CDP 连跑着的开发版、走真实 IPC）量到 `book.delete` 之后 highlights/cards 仍在库里、`highlightsDb.delete` 之后 cards 仍在，而 `tests/cascade-delete.test.ts` 全绿。② **逐层缩小差异**：库文件里 FK 声明与 `ON DELETE CASCADE` 都在（`PRAGMA foreign_key_list(highlights)` 读回确认）→ 同一个库文件复制到 node + sql.js 里按同样顺序删**级联正常** → 在应用里插两条临时日志：`fk after init = 1`、`fk before highlight delete = 0`。**外键是在运行中被关掉的**，不是没开。③ **根因**：`persistToDisk()` 每次落盘都调 `db.export()`，而 **sql.js 的 `export()` 会把这条连接上的 `PRAGMA foreign_keys` 复位成 0**（node 里三行代码就能复现：设 ON → `export()` → 回读 0）。`saveDatabase()` 是 3 秒防抖落盘 ⇒ 启动时开好的外键在第一次落盘之后就悄悄关了，此后所有级联删除都不生效，且**不报任何错**。④ **为什么 1100+ 条用例抓不到**：测试全走 `injectTestDatabase`，而 `persistToDisk()` 开头是 `if (!db || !isDirty) return` —— 模块级 `db` 在测试里永远是 null，**`export()` 这一步从未被测试执行过**（"测试绿"覆盖到的只是测试真跑过的那些代码）。⑤ **修法**：新增 `exportDatabaseForPersist()`（取完字节立刻重开外键），落盘只走它；`initializeSchema` 里那句 pragma 加注释指明"这里开一次不够"。**没有另加一份显式删子表的实现** —— 那会让数据完整性变成两套口径。⑥ **新守卫 `tests/sqljs-export-keeps-foreign-keys.test.ts` 5 条**：用 `setDatabase` 造一条生产形态的连接、把真实落盘走完整，再断言删书/删划线带走子行；含一条**反证**（裸 `export()` 之后 fk 变 0，钉住这个坑真实存在）与一条源码守卫（落盘路径不许出现裸 `db.export()`）。**反证做过**：把重开外键那行删掉 ⇒ 2 条判红；恢复 ⇒ 5 条全绿。⑦ **真机复测**（重启开发版）：删一条划线卡片一起没（1→0）；**等 4.5 秒让防抖落盘跑过一遍**再删一本书，划线与卡片都归零。⑧ **数据面清点**：开发库与装机版都只读查过一遍孤儿行（highlights / cards / reviews / knowledge_cards / methodologies / chat_messages 六类），**都是 0** —— 这个坑还没来得及攒下脏数据；探针自己留下的 1 张孤儿卡已按 `zz-%` 前缀清掉，开发库回到 92 书 / 934 划线 / 934 卡 / 10 复习。⑨ **对账**：`npm run verify` 退 0（**74 文件 / 1138 用例**，覆盖率 87.43 / 84.58 / 87.64 / 87.43，被统计文件仍 48），CHANGELOG 的 `[Unreleased]` 补齐前一批没写的条目（措辞按对外口径，只说改了什么问题）。**没验的**：❌ 装机版没装过这一版（出包通路仍卡着，见上一批与 #1）；❌ 界面上删除/撤销的一次真实点击（`tests/` 里的组件测试只断言按钮渲染与被点中）。 | AI Agent（接手） |
| 2026-09-24（功能深化批次） | **八项功能深化 + 实测出一条运行期缺陷** —— ① **生成的东西要能走回原处**：AI 回复的引用来源点得回那条划线（新增 `src/shared/source-anchor.ts`：深链拼法、DOM 锚点、参数读取三件事只有一份，改名字测试立刻红）；知识卡片与方法论摆出真出处并一跳回原文（顺手抓到方法论那栏**标题写「来源划线」、显示的是自己的说明文字**）；收藏的 AI 回复给跨会话出口（抽屉「收藏」页签 → 切会话 + 滚到那一条，此前收藏只写在单条消息上，换个会话就找不回）。② **对话上下文补两类数据**：新增文章与生词两个 BM25 构建器（优先级 55/45，排在读者自己的笔记之后），此前只有划线/卡片/方法论。③ **练习记账改由用户的动作决定** —— 原来是在 AI 回答文本里找方法论名，命中就 `practice_count+1`（模型夸口一句就算练过）。④ **单条划线可改可删、一本书能从书架删**（此前只有「重置整库」）。⑤ 三处假口径：`cards.due` 用 `due <= datetime('now')` 比较（ISO 串带 `T` 与空格，字符串比较下今天到期整天不计）、复习拉满 100 张就宣布完成、书架同步全失败仍弹「已是最新」—— 全改成参数化比较与如实计数。⑥ **`7030182` 删除先留现场、8 秒可撤销**：新增 `electron/services/deleted-archive.ts`（四类白名单 highlight / knowledge_card / methodology / vocabulary，划线连它的 cards 与 reviews 一起存；只在内存、最多 20 份、token 一次一废；`runTransaction` 里 `INSERT OR REPLACE` 整批塞回），提示条加可选动作按钮（带动作的默认留 8 秒，4 秒不够伸手点），随之**砍掉 5 条渲染层零消费者的物理删除通道**（含此前就没人用的 `cards:delete`）⇒ 通道 163 → 161，删除只剩一条会留现场的路。⑦ **⚠️ 本轮最值钱的一条是实测出来的**：dev 里 `remote-debugging-port 9222` 开着，用 node（24 自带 `fetch` + `WebSocket`）发 `Runtime.evaluate` 让**真实渲染层**调 `window.electronAPI.*`，量出**外键级联在运行期不生效** —— `book.delete` 之后 highlights/cards 仍在库里，`highlightsDb.delete` 之后 cards 仍在；同一个库文件复制到 node + sql.js 里按同样顺序（`PRAGMA foreign_keys = ON` → 建 → 删）级联**正常** ⇒ schema 声明与 `initializeSchema` 都挑不出毛病，差异在那条运行期连接上，**根因未定位**，已开任务跟（删除侧改显式删子表，不把数据完整性押在级联上）。探针在开发库里留下的 4 张孤儿卡已手工清干净（库回到 92 书 / 934 划线 / 934 卡 / 10 复习）。⑧ **对账**：测试 **64 文件 / 1071 用例 → 73 文件 / 1133 用例**，`npm run verify` 退 0（覆盖率 87.42 / 84.57 / 87.61 / 87.42，被统计文件 45 → 48），两处反证做实（现场少收一张表 → 3 条判红；`RESTORE_DELETE` 通道名打错 → 2 条判红）。**没验的**：❌ 撤销那颗按钮在真界面上的观感没点过（组件测试只断言它渲染出来并被点中）；❌ 装机版没跑过这一批（都不动打包链）；❌ 级联缺陷的根因。 | AI Agent（接手） |
| 2026-09-24（工具链批次 A1） | **Vite 5.4.21 → 6.4.3、electron-vite 2.3.0 → 5.0.0（Issue #12 的第一档）** —— ① **先量链条**：`vitest@4.1.11` 把 vite 变成 peer 且要 `^6||^7||^8`，而 `electron-vite@2.3.0` 的 peer 只到 `^5` ⇒ **必须先把 electron-vite 升到 5**（它的 peer 是 `^5||^6||^7`，也就是说升完还能停在 vite 5，两步可以分开走）。`electron-vite@5` 自带 `esbuild ^0.25.11` ⇒ 那条 esbuild 告警（要 >0.24.2）**不用 overrides 就自然清掉**。② **`@vitejs/plugin-react` 刻意不动**：实测 `4.7.0` 的 peer 是 `^4||^5||^6||^7`，vite 6 在它覆盖范围内；而它的最新版 peer 是 `vite ^8` —— **跟着"最新"走会白白拽一次大版本**。③ **实测落地**：锁里 `vite 6.4.3 / electron-vite 5.0.0 / esbuild 0.25.12 / vitest 3.2.7（没动）/ electron 39.8.10（没动）`；`npm run verify` 退 0（**64 文件 / 1071 用例**，覆盖率 86.33 / 84.24 / 84.82 / 86.33，阈值仍过）；`npm run dev` 真起一次：`Local: http://127.0.0.1:5500/` → `IPC handlers registered` → **`Main window shown`**。④ **产物形态逐项核过**（vite 换代最容易悄悄少打东西）：`dist/renderer/index.html` 仍是**相对路径** `./assets/...`（`file://` 下能加载）、字体 **105 个 woff2** 还在、`echarts-vendor` / `recharts-vendor` 两个手动分包都在、`dist/main/main.js` 396,910 字节与 `dist/preload/index.js` 28,902 字节都产出。⑤ **覆盖率数字有个 0.13pp 的挪动**（86.2 → 86.33）：同一批源文件、同一套断言，唯一变量是 esbuild 0.21.5→0.25.12 转译出的行映射不同 —— **不是代码变了，是覆盖率的计数口径跟着 sourcemap 抖了一下**，别读成"这次改动提高了覆盖率"。⑥ **⚠️ 本批最大的没验项：安装包没能重新出一次。** 实情是**本机 electron-builder 现在根本下载不到它的工具链二进制**：`~/.cache/electron-builder` 与 `~/.cache/electron` 都是空的（之前出包留下的 `installer-test/` 等目录也已被清理），不带镜像时 `connect ETIMEDOUT 20.205.243.166:443`（GitHub 下载主机，本机老毛病），带 `ELECTRON_BUILDER_BINARIES_MIRROR` 时换成 `Cannot read properties of undefined (reading 'ReadWrite')`（app-builder-lib `binDownload.js:27` 取 `@electron/get` 的 `ElectronDownloadCacheMode`）。**做了反证 bisect 才敢说与 vite 6 无关**：把 `package.json`/锁退回升级前（vite 5.4.21 / electron-vite 2.3.0 / esbuild 0.21.5）重装后跑同一条命令，**两种模式下报错一字不差**；又比对两份锁里 `@electron/get` 的 hoisting（顶层 2.0.3 + `app-builder-lib/node_modules` 里 3.1.0）**完全相同** —— 所以不是这次刷出来的。❌ 因此「vite 6 打出来的包能正常启动、asar 内容完整」这句话**这轮没说**，要等出包通路修好再补一次（与 #1 装机实测同属一类）。⑦ **顺手量到但刻意没做**：`glob` 那条告警的嵌套副本（`test-exclude/node_modules/glob@10.4.5`，要 ≥10.5.0）—— `10.5.0` 确实在 `^10.4.1` 范围内，但 `npm update glob` 与 `npm update test-exclude` 都不动它，而给一个 dev-only 的覆盖率传递依赖加 `overrides` 强拉版本正是本项目反复警惕的"刷绿灯"做法 ⇒ 不动，留给 #12 的 A2（vitest 4 会带来新的 `test-exclude`）。⑧ **另一条环境事实**：`npm ls` 暴露本地树曾被 pnpm 布局污染（`.pnpm` 目录 + `vite@5.4.21` 指向 `.pnpm/vite@5.4.0`），且 `@vitest/mocker` 是 extraneous —— 用 `npm ci` 清成与 CI 同构的树后 `.pnpm` 消失、门禁全绿；**但那次 `npm ci` 后我又跑了 `npm install`（为了升版本），所以不能断言污染已根治**，只断言了当前树里 `app-builder-lib` 只有一份、嵌套 `@electron/get@3.1.0` 在位。| AI Agent（接手） |
| 2026-09-24（文档指针批次） | **收掉 AGENTS / CLAUDE 里指向不存在文件的引用（Issue #7 做完）** —— ① **扫全量而非只修点名的三条**：`grep -n "\.claude/\|\.trae/"` 两条主文档共 **18 处**命中，但**不能一把删** —— 三类是合法用法必须留着：`infra-agent` 的可写范围 glob `.claude/**`（那是权限声明不是文件指针）、§4.3 结尾与 §2 目录树的裸 `.claude/`（说明它被 gitignore 排除，正是"别去找"的依据）、§十 变更记录里 2026-07-20「加入 .claude/」与 2026-09-11「标注未落地」两行（**append-only 历史，当时的事实不许改**）。真正要收的是 6 处"叫人来读一个不存在的文件"：ownership.yaml、review-agent.md、三份 `rules/*.md`、STANDARDS.md 正本、`.trae/specs/` 四件套 + §十 两行"待补"。② **方向选"收指针"而不是"补正本"**（issue 里推荐的那个）：这些目录全在 `.gitignore` 内，写出来也不入库，公开仓库里留引用就是永久断链；而且**本项目已被"两份口径各自漂移"咬过三次**（版本号 / 色值 / IPC 通道），再手写一份规范文本等于造第四份漂移源。改法是给每个领域指名**谁在强制**：§5.3 的表 = 15 条规则正本，§六 三行 = `eslint.config.js` + `tsconfig.json`（代码风格）、`CLAUDE.md` §2 + `SECURITY.md`（安全）、`commitlint.config.js` + `.husky/`（Git），§9.4 只留真正沉淀经验的地方（`.learnings/LEARNINGS.md` 的 LRN 编号）。③ **新增 `tests/doc-pointers.test.ts` 17 条**：四个死路径在**正文**（第十节之前）零命中；文档新指向的 10 个入库配置必须 `existsSync`。`.learnings/` 与 `.workbuddy/` 是本地文件，**不能断它们存在**（CI 上 fresh clone 就没有），改成断两件事：`.gitignore` 确实排除了它们 + 文档把它们标注成本地文件。④ **反证做过**：往 §六 临时注入一句"详见 `.claude/rules/code-style.md`" ⇒ 守卫立刻判红（错误信息带文件名与死路径），删掉恢复 ⇒ 17 条全绿。⑤ **通用判据试过但放弃**：issue 建议"文档里每个反引号路径都要 `ls` 得到"，实测量出 **34 处误报**（`main.ts` / `db-mapper.ts` 这类简写、`@/` 别名、`get()/getAll()` 这类非路径 token、npm 包名），照它写守卫会得到一条天天误报的测试 —— **判据宁要准不要全**，已记进 issue 评论。⑥ **顺手统一口径**：§9.3 测试行"门禁跑 `npm run test`"改成 `npm run verify`（上一批 CI 已改跑 `test:cov`，别让文档停在旧口径）；§9.3 文档行的"四件套"改为指名真正的沉淀位置；CLAUDE §8 的"规范基础设施 ⚠️"改为 ✅ 并写清"不再另立规范文本"。⑦ **对账**：新增 17 条 ⇒ **64 文件 / 1071 用例**（改前 63 / 1054），`npm run verify` 退 0，文档当前口径五处同步。⑧ **没验的**：❌ 没再让"新人照文档找文件"复现一遍（判据是脚本，不是人）；❌ `docs/` 目录（不入库）里的同类断链没扫。| AI Agent（接手） |
| 2026-09-24（覆盖率批次） | **把覆盖率阈值从装饰接成真的门禁（Issue #9 做完）** —— ① **根因**：CI 的测试步骤跑的是 `npm run test`（不带 `--coverage`），而阈值写在 `vitest.config.ts` 的 `coverage.thresholds` 里 —— **只有跑带覆盖率的命令才会评估它**。也就是说 lines 83 / branches 80 / functions 75 / statements 83 这四个数从写下起在 CI 上从没把过关，谁忘了在本地手动跑 `npm run test:cov` 就没人知道。② **修法**：CI 测试步骤换成 `npm run test:cov`（阈值不过 ⇒ 流水线红），覆盖率报告 `coverage/index.html` + `coverage-summary.json` 作为 artifact 留 14 天；`npm run verify` 同步换成 `test:cov`，**本地一条命令与 CI 完全同口径**（husky pre-commit 仍跑快的 `npm run test`，没拖慢提交）。③ **清掉两处静默空匹配**：清单里的 `electron/database.ts`（已拆成 `electron/database/` 14 个领域文件）与 `electron/services/http-client.ts`（已移到 `electron/http-client.ts`）**文件根本不在了**，vitest 对不匹配的 glob 不报错 ⇒ 白写两条、还让人以为覆盖了两处。前者改成 `electron/database/*.ts`（`index.ts` 由既有 exclude 挡掉），后者改真路径。④ **顺手把已有测试没进清单的补上**：算了一遍"被测试 import 的源文件"= **62 个**，而清单实际只量到 18 个 ⇒ 补进 10 个有专属测试文件的纯逻辑模块（`retrieval` / `daily-tasks` / `profile-stats` / `chapter-summaries` / `review-export` / `reading-trend` / `update-notice` / `csv` / `model-routing` / `settings-secrets`）。**只有常量与类型声明的不列**（`types.ts` / `ipc-channels.ts` / `external-links.ts`）—— 它们没有分支可盖，列进去等于往分母塞一个 0%。⑤ **实测（每一步都单独跑，不是推的）**：清单 20 条 / 存在 18 → 修正后 35 个文件（聚合 85.25 / 82.09 / 83.56 / 85.25）→ 再补纯逻辑后 **45 个文件、聚合 86.2 / 84.24 / 84.82 / 86.2**，仍全过阈值（余量 lines +3.2、branches +4.24、functions +9.82）。`npm run verify`（新口径）退 0。⑥ **文档口径全改**：AGENTS §2 目录树 / §3 命令 / §5.1 门禁表 / §5.2 CI 步骤、README 概览表与命令段与 CI 说明、CONTRIBUTING、PR 模板 —— 原来都写着"CI 不产生 coverage / 覆盖率是可选的 / 只作用于 25 个文件"，其中 **README 那句"25 个"两头都不对**（清单 20 条、真实存在 18 条）。⑦ **没验的**：❌ CI 上跑 `test:cov` 是否真的绿 **只能等推上去那一次**（本地是同版本同配置的 Windows 实跑，Runner 的 CPU 数不同会让 v8 覆盖率的极少数分支计数有差，历史上没遇到过但没排除）；❌ 阈值**没往上调**（余量 3.2 个百分点摆在那，那是下一个决定，不混在这批）；❌ 剩下 17 个"有测试却没进清单"的文件多为重逻辑（`orchestrator` / `weread-api` / `repositories` / 各 service），列进去会因未测分支拉低聚合 —— 本批刻意不动，欠账记进 #9 的评论。| AI Agent（接手） |
| 2026-09-24（质量批次） | **`tests/` 纳入 typecheck 覆盖，清零积压的 61 处类型错误（Issue #6 做完）** —— ① **先量再做**：把 `tests/**/*.ts` 加进根 `tsconfig.json` 的 `include`，`npx tsc --noEmit` 报出 **61 处 / 15 个文件**（这些文件此前完全在类型检查之外，只有真跑 vitest 才可能暴露问题，而 vitest 只看运行时、不看类型）。② **判据不是"清零"，是每条分清"测试写错类型"还是"被测代码真有谎"** —— 后者有三处，都改在生产侧：`renderTemplate()` 的实现按"缺值"处理 `null`（第 4 行明写 `value === null`）而签名不含它，于是任何拿数据库行（可空列读出来就是 `null`）的调用方都得绕类型；`AIServiceConfig` 被 `setAIConfig` / `getAIConfig` / `testConnection` 三个导出函数的签名使用却没 `export`（外面根本没法类型化地传参）；三个上下文构建器把 `shouldBuild()` 的接口形参省掉了，测试通过具体类调用时 TS 判"不接受参数"。③ **照出来一件不是缺陷但值得知道的事实**：`book` / `methodology` / `knowledge-card` 三个构建器的 `shouldBuild()` **无条件返回 true**，所以那 5 条用例传的上下文全被忽略。它们是 2026-09-16 那次"默认对话零上下文"修复的回归守卫（钉住不许再退回按 `bookId`/意图 gate），保留原样、只把签名对齐接口。④ **清零手法一律"补类型不动断言"**：`vi.fn(() => [])` 会推成 `never[]` ⇒ 后面 `mockReturnValue(真行数据)` 全判红、`mock.calls[0][1]` 也没人盯，改成显式标注返回类型（与生产签名逐字对齐）；`let capturedError: Error | null = null` + 回调里赋值 ⇒ TS 的 narrowing 停在 `null`，`capturedError?.message` 被判成 `never`（这点单独拿 `.test-tmp` 里的最小复现验过：`let` 形式报错、对象持有形式不报错），改成 `const captured: { error: Error | null }` 而不是加 cast；要比字段的返回值先 `if (n === null) throw` / `expect(x).not.toBeNull()` 再取（**只加检查，不减检查**）。⑤ **顺手收窄一处**：`dueCard(over: Record<string, unknown>)` 改成 `Partial<DueReviewCard>` —— 原来传错卡片字段名不会有任何提示。⑥ **新增 `tests/typecheck-coverage.test.ts` 4 条钉住覆盖面，反证做过**：临时把 `tests/**/*.ts` 从 `include` 删掉 ⇒ 2 条判红；恢复 ⇒ 4 条全绿（否则"守卫常在"不等于"守卫有用"）。⑦ **对账**：`npm run verify` 退 0，**63 文件 / 1054 用例**（改前 62 / 1050，多出的 4 条就是新守卫，一条没少），`tsc --noEmit` 错误数 61 → **0**。⑧ **顺手拆掉自己上一批埋的雷**：`secret-storage.test.ts` 与 `secret-ipc-boundary.test.ts` **共用同一个 `.test-tmp/user-data`**，两边都在 `beforeEach` 里 `rm` `settings.json` 与 `secure/` —— vitest 按文件并行，两个文件互删对方现场就是等着偶发红。改成 `tests/electron-mock-setup.ts` 认 `ZHIXING_TEST_PROFILE`、两个文件各占一个 profile 目录（目录名与该文件的 `DATA_DIR` 由同一个常量派生，不会漂）；两个文件同跑 28 条全绿、全套连跑退 0。⑨ **一条偶发红的取证（不是本批引入）**：pre-commit 那次跑挂的是 `MessageBubble.test.tsx:430` 的 `toHaveTextContent('cellContent')`（该用例依赖 React 对 `<td>` 直接挂在 `<div>` 下的纠错行为，运行时警告本来就写着 "cannot be a child of a &lt;div&gt;"），随后 **4 次全套连跑全部退 0** —— 没复现、也没去动它，归到渲染测试那一摊（#8），别读成本批的回归。⑩ **没验的**：❌ 本次只保证"断言语义一字未动"，**没有逐条复核这些测试断言本身对不对**（覆盖的是编译期，不是正确性）；❌ 测试文件仍不在覆盖率 include 里（那是 #9）；❌ 组件测试（`.tsx`）本来就已在 `src/**/*.tsx` 覆盖内 —— 这点是量过的：61 条错误里没有一条来自 `.tsx`，不是猜的。| AI Agent（接手） |
| 2026-09-23（安全批次 2/2） | **密钥原值不再跨进程下发：界面只知「配没配」** —— ① **上一条把盘堵住了，这条堵的是进程间那条缝**：`SETTINGS.GET_ALL` 走的是 `settingsService.getAll()`，而它会把**解密后的 key 原值**一起发给渲染层，设置页再回填进输入框 —— 每次打开设置就把刚加密的东西摊开一次（渲染层任意一处 XSS、DevTools、任何后来加的日志都拿得到）。② **口径改成「只传布尔」**：新增 `settingsService.getForRenderer()` —— 密钥字段从对象里删掉，换成 `wereadApiKeySet` / `llmKeySet`（字段名唯一真值在 `secretSetFlagName()`）；`SETTINGS.GET` 问到密钥键名**直接抛错**而不是回 `undefined`，因为回空值会被界面读成"没配"、随手一次保存就把用户的 key 清掉。③ **输入框语义改成"留空即不修改"**，配不上的时候占位文案说清楚（`已保存（留空则不修改）`）；`setSecureKey(key, '')` 变成**显式清除**（密文文件与明文残留一起抹掉），界面旁边给了「清除已保存的 Key」按钮 —— 没有这颗按钮的话"清空输入框"就不再等于删除，用户会失去退出通道。`settings.set(key, 非字符串)` 一律拒绝写入（防一次手滑的 `undefined` 抹掉 key）。④ **顺手修掉一条被暴露出来的真断链**：`ai.setConfig` 过去整个包在 `if (llmKey)` 里 ⇒ **"只改端点或模型、不重填 key"这条最常见的保存路径根本不会把配置下发给 AI 服务，要重启才生效**。现在无条件调用，apiKey 留空时由主进程补回已存的那把（`withStoredApiKey()` 放在 `src/shared/settings-secrets.ts`，判定逻辑可单测）；`weread.test('')` 本来就有 `key || apiKey` 回退，所以测试连接同样能用上已存的 key。⑤ **砍一条随之变死的通道**：`WEREAD.SET_API_KEY`（通道 + handler + preload + `renderer.d.ts`）—— 渲染层唯一的用途就是把 key 再塞一次进主进程内存，而 `SETTINGS.SET` 的 handler 早就在做这件事。通道数 164 → **163**（按 `ipc-channels.ts` 里去重后的字面量数实测，同一套算法喂 `HEAD` 版本得到 164，判据本身验过）。⑥ **实测（15 条新用例，`tests/secret-ipc-boundary.test.ts`）**：直接注册**真实的** `registerSettingsHandlers` 再调 handler，而不是只看源码 —— 存着原值的现场里，交出去的对象 `JSON.stringify` 查不到那串值（**反证**：同一个 `settingsService.getAll()` 查得到，证明"查不到"是边界生效不是没存进去）；未配是 `false` 不是缺字段；非密钥项照常下发（过滤没把设置页掏空）；`GET('wereadApiKey')` 抛错且错误信息里不含原值；空串清除后 `.enc` 与 `get()` 都没了、flag 变 `false`；`undefined`/`null` 抹不掉已存 key；`withStoredApiKey` 三档（空/缺失补、填了不顶、主进程也没有就交回空串）；渲染层源码扫描两处旧读法不许回来（配"扫描看得见真东西"的反证）。**门禁**：`npm run verify` 退 0（**62 文件 / 1050 用例**，eslint 0 error / 176 warning，typecheck 0 错误，三进程 build 成功）。**没验的**：❌ **界面一张没点开过** —— 「清除已保存的 Key」的实际观感、两种占位文案、点清除之后"已连接"徽标是否如期变灰，全都只有代码推理；❌ 装机版没重跑（这批不动打包链）；❌ **`.enc` 解不开时设置页长什么样**没实测：按推理 flag 会是 `false`、界面显示"未连接"、用户重填即覆盖，但这条路径（换机器/重装系统）本机造不出来；❌ "只改模型不下发"那条修复只过了单测与 build，没在真 AI 服务上验证过一次保存后立即生效。| AI Agent（接手） |
| 2026-09-23（安全批次 1/2） | **接通 API 密钥的系统加密存储（Issue #13 的方向：接通，不是砍）** —— ① **先确认现状有多糟**：`getSecureKey` / `setSecureKey` 自写下起**零调用方**，所有密钥明文躺在 `%APPDATA%\zhixing-reader\settings.json`；界面那句"系统加密不可用时才明文"其实一直是反的（加密通路根本没接）。② **一张表管住读写**：新增 `src/shared/settings-secrets.ts` 的 `SECRET_SETTING_KEYS = ['wereadApiKey','llmKey']`，`set/get/getAll` 三条路都从它判 —— 以后加新的密钥类设置项只改这一处。**加 API Key 类设置项必须同步进这张表**，否则又会变成明文。③ **失败路径一律偏保守**：加密不可用、`encryptString` 抛错 ⇒ 退回写明文（用户的 key 不能因为 DPAPI 拒绝访问就没了）；`decryptString` 抛错 ⇒ 返回 null 且**留着 .enc 不删**。④ **可用性不看布尔值，做一次自检往返**：`isEncryptionAvailable()` 在换机器/DPAPI 拒绝时仍返回 true，所以 `isEncryptionAvailable()` 里加了一趟 `encrypt→decrypt` 自检，界面那句提示与 `secureStorageAvailable` 都跟着它走。⑤ **实测（13 条新用例 + 真跑一次）**：`tests/secret-storage.test.ts` 13 条 —— 含"探针能过但真值加密抛错""探针能过但真值解密抛错"两条专门防自欺的（只测"探针失败"会漏掉真正的失败路径）；迁移幂等、非密钥项不许被搬进 `.enc`、`getAll` 与 `get` 口径一致。**迁移结果的口径按落盘后的实际状态判定**（报"已迁移"就必须明文从 `settings.json` 消失且密文文件真存在），半途失败只报 `keptPlaintext` —— 第一版按"有没有抛错"判定，撞上"密文已存在但加密又失败"时会对着日志谎报成功，改判据时补了第 13 条用例钉住。**真机验证**：开发版启动后 `secure/{wereadApiKey,llmKey}.enc`（59 / 66 字节）生成、`settings.json` 里两个字段消失、日志出现 `Migrated plaintext secrets to encrypted storage` 与 `WeRead API Key loaded from settings`、第二次启动迁移计数为 0（幂等）且 key 仍解得开；另用一次性 Electron 探针把密文解回原值，**长度 28 / 35 与迁移前一致**（没打印任何内容）。顺带答掉 #11 留的那个问号：**`safeStorage` 在 Electron 39 上真的能加解密**。⑥ **横扫同类**：日志 `SENSITIVE_KEY_PATTERN` 实测命中 `wereadApiKey` / `llmKey`（会打码）；数据导出只打包书籍/划线/卡片/方法论/生词，**不含设置项**；备份恢复没有把 `settings.json` 装进 payload 的通路。⑦ **门禁**：`npm run verify` 退 0（**61 文件 / 1035 用例**，eslint 0 error / 176 warning，typecheck 0 错误，build 成功）。⑧ **本机善后**：迁移前把开发版 `settings.json` 备份成 `.bak-preecrypt-20260923`，密文验证解得回原值后**已删除该备份**（留一份明文备份等于白干）。**没做完的在下半场**：`SETTINGS.GET_ALL` 仍会把解密后的密钥原值发到渲染层、并回填进输入框 —— 落盘加密了但进程间又摊开了一遍，下半场（安全批次 2/2）改成"界面只知有没有配、留空不修改"。| AI Agent（接手） |
| 2026-09-23（打包链批次） | **electron-builder 25.1.8 → 26.15.3，清掉 10 条告警并重新出包实测** —— ① **换代点先对清单再看能不能升**：官方 26.0.0 的破坏项里，`win` 签名配置移进 `win.signtoolOptions`（本项目不签名，日志一直是 `no signing info identified, signing is skipped`）、Linux `.desktop` 改对象（只打 Windows）、`electronDist` 转 Hook（没用）、`fpm` 默认依赖从 app-builder-bin 搬进 JS —— **都不落在我们用的配置上**；唯一真吃得到的是「asar 打包改用官方 `@electron/asar`」，因为我们 `asar: true` + `asarUnpack: [dist/**, sql.js 的 wasm]`。② **升完先看锁里落到哪**：`app-builder-lib 26.15.3` / `builder-util-runtime 9.7.0`（两条 high 正好达标），而 **`tar` 从 6.2.1 直接跳到 7.5.22** —— 那 8 条 tar 告警是跟着一起清掉的，没动 `overrides`。③ **重新出包核内容**（asar 换代最容易悄悄少打东西）：`app.asar` 内 **12,220 个文件**、字体 **105 个 woff2**、wasm 5 个，`app.asar.unpacked/node_modules/sql.js/dist/sql-wasm.wasm` 实测 **659,730 字节**还在，`win-unpacked` 总量 521,821 KB。产物 `zhixing-reader-Setup-1.3.4.exe` **127,839,070 字节**，比 25.1.8 打的那次小 7 MB —— ⚠️ **原因我没查**（我们没配 `compression`，26 的默认值变了是唯一候选解释，但没证实；内容完整性是量过的，不是靠"变小所以变好"）。④ **打包版真跑了一次**：`installer-test/win-unpacked/知行读书.exe` 日志出现 `Found preload at ...` → **`Main window shown`**，而且这回静默检查更新走通了，如实回报 `{"stage":"not-available","version":"1.3.4"}`（上一轮是 `ERR_CONNECTION_RESET`，网络问题不是代码）。⑤ **门禁**：`npm run verify` 退 0（**1022 用例 / 60 文件**，eslint 0 error / 176 warning，typecheck 0 错误，三进程 build 成功）。⑥ **告警对账**：GitHub open 20 → 按新锁文件重算**只剩 10 命中**（vite 3、vitest + @vitest/mocker 各 1、esbuild 1、glob 1、echarts 1、**extract-zip 2 上游无补丁**）；`glob` 那条仍是 `test-exclude/node_modules/glob@10.4.5` 这个嵌套副本，**别以为升过了**。⑦ **没验的**：❌ 没做一次真正的 NSIS 安装（产物只以 `win-unpacked` 形态跑，#1 仍未动）；❌ 安装包变小 7 MB 的原因；❌ 界面一张没点开看（本批不动渲染）。| AI Agent（接手） |
| 2026-09-23（工具链批次） | **Vitest 2.1.9 → 3.2.7，清掉唯一那条 critical 告警；刻意不碰 vite** —— ① **先量依赖链再动手**：`npm view vitest@3.2.6 dependencies.vite` = `^5 || ^6 || ^7-0` ⇒ 3.x 自带 vite 依赖、**和我们的 vite 5.4.21 不冲突**；而 `vitest@4.1.11` 把 vite 变成 **peer** 且要求 `^6 || ^7 || ^8`，一升就要连带动 vite 6（`electron-vite@2.3.0` 的 peer 只到 `^5`，得再升 electron-vite 5）。所以这一批只做「代价最小、把 critical 摘掉」的那一步，#12 剩下的按链条分层另做。② **顺手去掉一处双口径**：`vitest.config.ts` 的 `environmentMatchGlobs` 在 Vitest 3 已标废（4 会删），而它和测试文件首行的 `@vitest-environment` **同时在指环境** —— `admin-charts.test.tsx` 就是被 glob 指到 happy-dom、自己第 24 行又声明了 jsdom 的那类，谁生效说不清。改成「每个文件自己声明」这一套口径，给 `Modal.test.tsx` 补上 docblock 再删掉那个选项。③ **实测**：`npm run test` 60 文件 / **1022 用例**全通过（数字与升级前一致，四个 tsx 组件测试 Modal 8 / RetrievalPanel 5 / MessageBubble 69 / admin-charts 45 全绿），`DEPRECATED` 警告从每次跑都出现变成零；`npm run test:cov` 退 0（All files 91.84/83.3/95.58/91.84，阈值 83/80/75/83）；`npm run verify` 退 0（eslint 0 error / 176 warning，typecheck 0 错误，三进程 build 成功）。⑤ **告警对账**：85 → 上一批 64 清 → 这批再清 1 条，**剩 20 条**（vitest+@vitest/mocker 各 1 要 4.1.11、vite 3 要 6.4.3、tar 8 要 7.5.18、electron-builder 侧 2 要 26.15.0/9.7.0、esbuild 1 要 0.25.0、glob 1 要 10.5.0、echarts 1 要 6.1.0、**extract-zip 2 条上游无补丁**）。⑥ **文档口径同步**：AGENTS/CLAUDE/README 里「当前 1020 用例」改成实测 1022、README 的 Vitest `2.x` 改 `3.x`（含 tests 徽标里的数字）；**各批次变更记录里那些当年的 1020 是历史实测值，一个字没动**。⑦ **没验的**：❌ 只验了测试与构建，没验界面表现（这批不动运行时与产物）；❌ vitest 4 + vite 6 + electron-vite 5 那一档**没做**，风险与收益都留在 #12 里；❌ 并行稳定性只跑了单轮 —— 老记录说旧版本有并行崩溃问题，这次没有连跑多轮取证。| AI Agent（接手） |
| 2026-09-23（运行时批次） | **Electron 35.7.5 → 39.8.10（Issue #11 做完），并顺带挖出开发版数据目录的一条时序缺陷** —— ① **为什么停在 39**：把 GitHub 那 33 条 electron 告警（含上一批已满足的 1 条）的 `first_patched_version` 逐条拉下来排，最高要求正好是 **39.8.10**（1 条要 35.7.5、13 条要 38.8.6、19 条要 39.8.x：39.8.1×1 / 39.8.5×3 / 39.8.6×1 / 39.8.7×2 / 39.8.8×6 / 39.8.9×3 / 39.8.10×3，严重度 medium 20 / high 7 / low 6）；只升到 38.8.6 清不掉后面那 19 条。`package.json` 声明由 `^35.0.0` 改成 `^39.0.0`。② **二进制下不来**：`npm install` 的 postinstall 直连 GitHub 超时（`connect ETIMEDOUT 20.205.243.166:443`），而 `~/.npmrc` 里那条 `disturl` 早就不被读（npm 自己都在警告 unknown config）—— 改 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/` 走镜像，137 MB / 8.5 秒。装完照旧单独验：`node_modules/electron/dist/version` 与 `electron.exe --version` 都是 **39.8.10**。③ **破坏性变更逐条对着官方 breaking-changes 过 36/37/38/39**：36 的 `app.commandLine` 转小写（我们两个 switch 本来全小写）、36 的 `NativeImage.getBitmap()`→`toBitmap()`（**全仓库零调用**）、39 的 `window.open` 必建可调整弹窗（`setWindowOpenHandler` 恒返回 `deny`，走不到）、38 要 macOS 12 与 C++20（只打 Windows、且 sql.js 是 WASM 无原生模块）。36–39 **没有任何一条**落在本项目实际用到的 `app.getPath/setPath` / `app.exit` / `requestSingleInstanceLock` / `safeStorage` / `ipcMain.handle` / `contextBridge` 上。④ **重新出了包**：`electron-builder 25.1.8` 直接吃下 39.8.10（日志 `packaging platform=win32 electron=39.8.10`）。产物写到 gitignore 的 `installer-test/`（**故意不盖 `installer/` 里那份已发布的 1.3.4**），`zhixing-reader-Setup-1.3.4.exe` 实测 **134,881,884 字节**（线上那份 1.3.4 是 119,246,768，差的 15.6 MB 就是运行时变大的量），`latest.yml` 里的 sha512 与本机文件**重算逐字节一致**，随包 `app.asar` 内 `package.json` version 仍是 1.3.4。⚠️ 第一次出包在 NSIS 那步抛 `ENOSPC`：当时 D 盘还剩 58 GB、往三个候选目录各写 20 MB 全部成功 ⇒ 判为瞬时，**重试一次就过了**，别把它读成磁盘真满。⑤ **打包产物真跑过一次**：启动 `installer-test/win-unpacked/知行读书.exe`，装机 profile 日志里依次出现 `Database connected` → `IPC handlers registered` → **`Main window shown`**，新建 `zhixing.db` 241,664 字节；`app.isPackaged` 那条路也真走到了 —— 静默检查更新发出并回报 `net::ERR_CONNECTION_RESET`（本机到 GitHub 下载主机的老问题，见 §7 环境坑），**是网络不是代码**。⑥ **顺手抓到一条真缺陷**：开发版日志里始终没有「WeRead API Key loaded from settings」，而 `-dev` 目录的 settings.json 里 key 明明在。对比两个 profile 的写入时间发现：`logger` 与 `settings-service` 都在**模块加载期**就绑定了 `app.getPath('userData')`，而静态 import 全部早于 `main.ts` 本体执行 —— 09-21 那次数据目录隔离**只对数据库和单实例锁生效，settings 与 logs 一直写的还是装机版目录**。后果两个：开发版看不见用户配的密钥（界面报「请先设置」），且开发版每次改设置会盖掉装机版的设置。修法：切换逻辑提成 `electron/user-data.ts`，排成 `main.ts` 最靠前的 import；守卫从 3 条加到 5 条，新增的一条是**反证**（断言那两个模块确实在 constructor 里读 userData、且 export 时就实例化），并临时把 import 顺序改错一次实测它变红、改回又变绿。修完复跑 dev：日志落到 `zhixing-reader-dev/logs/`、`WeRead API Key loaded from settings` 出现、装机版两个文件的字节数与 mtime **纹丝不动**。**同类横扫**：`database/connection.ts` 与 `ipc/settings.ts` 是在函数里读 userData ⇒ 本来就对；`dictionary-service` / `knowledge-card-service` 构造期不读 ⇒ 不受影响。**善后**：坏路径那次在装机版目录写下的 81 字节 `settings.json`（只有 `secureStorageAvailable` 和一个时间戳，零用户设置）已删回原样；那次追加进装机版日志文件的行留着没删（日志是历史）。⑦ **对账**：同一套归一化范围算法喂进新锁文件，85 条告警现在**命中 21 条 / 清掉 64 条**，electron 归零；剩下 21 条正好就是 Issue #12 的清单（tar 8、vite 3、vitest 2、extract-zip 2、@vitest/mocker 1、builder-util-runtime 1、app-builder-lib 1、echarts 1、glob 1、esbuild 1）。⑧ **门禁实测**：`npm run verify` 退出码 0（**1022 用例 / 60 文件**，比上一批多的 2 条就是隔离守卫新增的，eslint 0 error / 176 warning，typecheck 0 错误，三进程 build 成功）。**没验的**：❌ 没做**真正的安装**（产物只以 `win-unpacked` 形态跑起来，NSIS 装→数据保留→卸载这条链仍未在新运行时上验过，那是 Issue #1）；❌ 应用内「检查更新→下载→重启安装」在新运行时上**没跑完过**（这次连不上下载主机）；❌ Chromium 从 128 跳到 39 对应的新版，**界面一张都没点开看**（渲染差异靠人眼，本项目不做视觉验证）；❌ `safeStorage` 在 39 上到底能不能加解密**没单测**——只看到两个 profile 的 `secureStorageAvailable` 都记成 `true`，而 `getSecureKey`/`setSecureKey` 全仓库**零调用方**：密钥实际一直是明文进 settings.json，那套 `.enc` 存储是死代码，已另开 Issue。| AI Agent（接手） |
| 2026-09-23（依赖批次） | **Dependabot 报的 85 条告警先做掉能安全做的 33 条** —— 推送时 GitHub 顺带报出默认分支有 85 条依赖告警，先按「是不是随包发出去的东西」分了一遍类再动手，而不是一把梭。① **本地 `npm audit` 用不了**：registry 指向 npmmirror，它的 `/-/npm/v1/security/*` 直接 404（`[NOT_IMPLEMENTED]`），所以告警数据只能取 GitHub 的，判据也是 GitHub 给的脆弱范围。② **分类结论**：21 个包里真正随包跑在用户机器上的只有 `react-router`（经 `react-router-dom`）、`echarts`、以及 `electron-updater` 带出来的 `js-yaml`/`builder-util-runtime`；其余 electron、vite、vitest、tar、@xmldom/xmldom… 全是构建期工具链。③ **只做 semver 范围内的一批**：`npm update`（**`package.json` 一个字没改**，只改锁文件，2739 增 / 1129 删），实测落地 electron 35.0.0→**35.7.5**、react-router 7.16.0→**7.18.4**、js-yaml 4.1.1→**4.3.2**、@xmldom/xmldom 0.9.10→**0.9.12**、postcss→8.5.28、nanoid→3.3.19、form-data→4.0.6、browserslist→4.29.0、ip-address→10.7.2、brace-expansion→5.0.12。**electron 的二进制真的换了**：`node_modules/electron/dist/version` 实测 `35.7.5`（npm 提示 postinstall 未被 allowScripts 覆盖，所以这一条必须单独验，不能假设）。④ **用锁文件重算对账**（脚本把 85 条告警的脆弱范围逐条 `semver.satisfies` 喂进当前锁里的**全部副本含嵌套**）：**清掉 33 / 85，仍命中 52**。⚠️ 这条数字差点报错：node-semver **不认 GHSA 那种逗号范围**（`>= 2.1.0, < 4.1.11` 用 `satisfies()` 对任何版本都返回 false —— 实测 `2.1.0`/`3.0.0`/`9.9.9` 三档全 false），第一版脚本因此把这类告警当成「已清除」，虚报 37 条清掉；把逗号归一化成空格（AND）再判才对。**教训：范围/正则一类的判据必须先拿「本该命中」的版本证它非空，否则扫描器永远绿**。剩下的 52 条全部要跨大版本才有解：electron 31 条要 38.8.6/39.8.x（严重度 high 7 / medium 18 / low 6）、tar 8 条要 7.5.18（high 6）、vite 3 条要 6.4.3、vitest + @vitest/mocker 3 条要 4.1.11（含 1 条 **critical**）、`app-builder-lib` 要 26.15.0、`builder-util-runtime` 要 9.7.0、echarts 要 6.1.0、esbuild 要 >0.24.2、**`glob` 的嵌套副本 10.4.5 仍在 `>= 10.2.0, < 10.5.0` 里（别以为升过了）**，`extract-zip` 2 条**上游根本没发补丁**。⑤ **故意没做的**：没给 `builder-util-runtime`/`tar`/`esbuild` 加 `overrides` 强行拉版本 —— 前者在自动更新解析 `latest.yml` 的路径上，而后者的效果只有真装一次才看得见，用 overrides 刷绿灯正是本项目反复警惕的做法。⑥ **顺手被带上去的两处要留意**：`echarts` 5.5.1→5.6.0 是自动升的（它是随包的 prod 依赖），图表只过了构建与单测，**界面一张都没点开看**。⑦ **门禁实测**：`npm run verify` 退出码 0（**1020 用例 / 60 文件**，eslint 0 error / 176 warning，typecheck 0 错误，三进程 build 成功）。**没验的**：没为这次依赖变动重新 `npm run package:win`（本地 `installer/` 仍是已发布的 1.3.4 产物，重跑会盖掉它），所以「35.7.5 打出来的包能正常启动、自动更新链路不受影响」这句话**没说**；升级后的 Electron 也没在装机版上跑过。剩 52 条列成 Issue #11（Electron 大版本 + 需重新出包与装机实测）与 Issue #12（构建工具链大版本 + 无上游补丁项）。| AI Agent（接手） |
| 2026-09-23（续） | **GitHub 维护一轮：CI 从 09-19 起每次推送都红的根因 + 协作面补齐 + 10 条 backlog Issue** —— ① **CI 根因是换行符，不是代码**：`tests/design-tokens.test.ts` 用 `scripts/build-tokens.mjs --check` 对两份调色板产物做**逐字节比对**，而 windows-latest Runner 的 git 默认 `core.autocrlf=true`，checkout 时把 LF 换成 CRLF ⇒ 产物永远被判「过期」。实测复现：`git clone -c core.autocrlf=true` 拉一份新克隆，`--check` 退 1、错误文案与 CI 逐字相同；加上 `.gitattributes`（`* text=auto eol=lf`）后同一份克隆退 0。**本地看不见这条**是因为本仓库把 `core.autocrlf` 设成了 `input`。最后一次绿的 CI 是 2026-09-18 15:04，而 `design-tokens.test.ts` 是 09-19 落地的 —— 时间线对得上，从此 20 次推送全红。新增守卫断言「`.gitattributes` 必须声明 `eol=lf` + 两份产物不得含回车符」，那行被人删掉时立刻判红。② **改前扫同类**：`git add --renormalize .` 之前先量了「跟踪文件里含 CR 的数量 = 0」且没有 `.bat/.cmd/.ps1` 入库 ⇒ 这条属性对本地零影响（husky 那两个 shell 钩子本来正需要 LF）。③ **补协作面**：`.github/ISSUE_TEMPLATE/` 三个模板（Bug / 功能建议 / 环境与构建）+ `config.yml` 关掉空白 Issue、`.github/PULL_REQUEST_TEMPLATE.md`（含「我验证过什么」必填空栏 + 本仓库 7 条守卫清单），CONTRIBUTING 里那份内联 PR 模板删掉改为指向文件（免得两份漂移），`SECURITY.md` 新增（私有上报渠道 + 现状安全边界，逐条对着代码核过：`contextIsolation: true` / `nodeIntegration: false` / **`sandbox` 是关的**、`redactSensitive()` 日志脱敏、`safeStorage` 不可用时明文如实告知、Secret Scanning + Push Protection 已由 API 确认开启）。④ **README 按代码逐条重核**：五维构建器那张表的「每维 Token 预算 1500/1000/800/500/200」在代码里**根本不存在**（真实机制是 `electron/agent/context-manager.ts` 一个全局 `MAX_CONTEXT_TOKENS = 4000` + 每维各自的取数条数上限），已按实测改表；「Token 节省 33%-55%」「本地决策链路 52-207ms」这类没有测量支撑的写法撤掉，第九节整节重写并给每行标口径（只留两行真测过的）；代码行数 52,000+ → **53,823 行实测**；IPC「12 个领域 handler」→ 11 个 + 164 条通道；`npm run test` 的说明原本写「含覆盖率」是错的，改成不含并单列 `test:cov`；目录树补 `utils/db-mapper.ts` / `.gitattributes` / `SECURITY.md` / `.github/`；删掉指向未入库 `diagrams/` 的死引用；新增「已知限制」与「反馈问题」两节，限制条目带 Issue 编号；补 CI 状态徽标。⑤ **`CODE_OF_CONDUCT.md` 两处硬错**：让举报人**在公开 Issue 里**举报骚扰（且模板上线后 `blank_issues_enabled: false`，压根开不了空白 Issue）、引用不存在的 `code-of-conduct` 标签 —— 改成「标题只写『行为准则举报，请转私享渠道』、正文留空」。`CONTRIBUTING.md` 里 4 处 `main` 改成 `master`（本仓库默认分支是 master，照着做的人会失败在第一步）。⑥ **顺手删一条死代码**：`electron/database/token-usage.ts` 的 `deleteOlderThan(days)` 全仓库零引用，而它是整个库里**唯一把值拼进 SQL 字面量**的地方（`datetime('now', '-${days} days')`），删掉之后「SQL 取值全走 `?` 占位符」这句才真说得出口。⑦ **建 10 条 Issue（#1~#10）**：装机实测缺口 / 无鉴权页 / 生词与文章缺映射层（31 处 `as unknown as` 的分布按文件实测）/ B1 最后两个 JSON 型函数 / macOS·Linux 打包与签名 / `tests/` 不在 typecheck 覆盖（根 `tsconfig.json` 的 `include` 只有 electron·src·scripts）/ AGENTS 引用的三处不存在正本 / 两个 store 零测试 + 14 页面零渲染测试（实测：0 个测试 import 页面组件）/ 覆盖率门禁接入（依赖版本实测已对齐 2.1.9，老注释说要先 pnpm）/ 发布产物未整份回下载重算 sha512。四条打 `good first issue`。**门禁实测**：`npm run verify` 退出码 0（**1020 用例 / 60 文件**，比上一批多的 1 条就是新增的 eol 守卫；eslint 0 error / 176 warning，typecheck 0 错误，三进程 build 成功）。**没验的**：CI 是否真转绿只能等推上去那一次 run（本地是拿「同条件新克隆」复现与反证，不是在 Runner 上跑过）；Issue 模板在界面上长什么样没看过（只验了四个 YAML 都能被 js-yaml 解析、body 分别 10/7/8 项）；**推送之后又回补了三件实测**：GitHub 社区健康度 100%，`issue_template` 与 `pull_request_template` 都被识别（模板不止 YAML 能解析，确实被认了）；私有漏洞报告**本来是关的**（`enabled:false`），已开成 true 并把直达链接写进 `SECURITY.md`；Dependabot 安全更新已开（`vulnerability-alerts` 由 404 变 204），仓库 homepage 也补上了。**topics 随后也补上了（12 条，隔几秒独立读回确认）** —— 这里有一条 API 坑值得记：`PATCH /repos/{owner}/{repo}` 带 `topics` 字段会**返回 200 但把字段静默丢掉**（读回永远是 0；限流还剩 4972/5000、令牌是 classic PAT 全 `repo` scope，两条常见解释都被数据排掉了）。正解是专用路由 `PUT /repos/{owner}/{repo}/topics`，**body 字段名叫 `names` 不叫 `topics`** —— 这点是拿错误体读出来的：字段名给错时它回 422 并直接写清 `"names" wasn't supplied.`。**判据：接口返回 2xx 但状态没变，先去要一次"故意传错"看它的 422 报错体，别急着归因到权限或频率。**CI 徽标现在有内容了（master success）。`npm run test:cov` 本轮仍没跑（那是 #9 的第一步）。| AI Agent（接手） |
| 2026-09-23 | **v1.3.4 出包并发布** —— 把 09-22 攒的四项修复（重启安装退出路径、开发版数据目录独立、ReviewStats 两份实现口径、渲染层行类型统一）打成可安装版本。走 `tests/version-sync.test.ts` 钉住的六处同步点（`npm version` / `APP_META` / CHANGELOG `## [1.3.4]` + 链接行 / README 五处 / 关于页 `UPDATE_HISTORY` 首条），`npm run verify` 退出码 0（**1019 用例 / 60 文件**，eslint 0 error / 176 warning，typecheck 0 错误，三进程 build 成功）。**产物实测**：`installer/zhixing-reader-Setup-1.3.4.exe` 119,246,768 字节（113.7 MiB，比 1.3.3 多 314 字节），`win-unpacked/知行读书.exe` FileVersion 1.3.4，**随包 `app.asar` 内 `package.json` 实测 `version: 1.3.4`**（关于页与「检查更新」读的就是它），`latest.yml` 的 version/size/sha512 与本机 exe 逐字节自洽、`dictionary.json` 随包字节与源文件相同。**已发布**：tag `v1.3.4` 已推、Release 标为 Latest、三件同传（线上报的 exe 119,246,768 / blockmap 124,651 / latest.yml 357 字节与本机一致），`latest.yml` 从线上回下载与本机产物**逐字节相同**。**踩坑一条**：`gh release create` 带三个资产一次跑完时，113 MB 的 exe 上传中途被网络打断后 gh 会自己重试，而第二次撞上 `422 ReleaseAsset.name already exists` —— gh 于是把刚建的 release 整个回滚删掉（`gh release view v1.3.4` 直接 not found，Latest 仍是 1.3.3）。改成**先 `gh release create`（不带资产）再 `gh release upload --clobber`** 两步，重跑就干净了。**没验的**：远端 exe 只核到「API 报的字节数 == 本机文件」，**没有整份回下载重算 sha512**（这台机器此刻 `github.com` 下载主机连不上：`curl` 报 `Recv failure: Connection was reset`，重试变成 21 秒连接超时；`api.github.com` / `uploads.github.com` 通路正常，所以 gh 全程能用）；装机版应用内「检查更新 → 下载 → 重启安装」这一整遭**仍未在新退出路径上真跑过** —— 这次发的正是那条修复，但它到底能不能装下来，只有装的时候才知道。| AI Agent（接手） |
| 2026-09-22（十续） | **渲染层 63 处「强行转类型」清到 32 处，顺带抓出五个库里根本不存在的列名** —— 根因只有一处：`utils/db-mapper.ts` 以前把所有行都返回 `Record<string, unknown>`，于是八个页面各自再声明一份 `BookRow/HighlightRow/CardRow` 然后 `as unknown as` 硬转。类型不报错，但也不兜底：**谁读一个库里没有的列，只有等界面上摆出一个永远为 0 的数字才看得见**。① 映射器改为导出 `BookRow / HighlightRow / CardRow / KnowledgeCardRow / MethodologyRow`（只声明「映射器保证写得出」的字段，其余列靠 `...row` + 索引签名透传），八个页面删掉自己的行类型改 import，`Home / Bookshelf / Notes / BookDetail / KnowledgeCards / Methodologies / Chat / Profile / profileStore / Stats` 共 10 处文件的重声明与硬转一并取消 —— typecheck 就是抓假字段的探测器，改一处报错点一处。② **当场抓出五个假读法**：`highlights` 表没有 `color`（恒返回默认色值）、没有 `chapter_id/chapter_uid`（章节只存名字）、**没有 `type`** —— 而书籍详情按 `h.type === 'note'` 筛笔记，所以「笔记」页签和「笔记 N 条」这个数**从有这页起就恒为 0**（微信读书的想法落库时只有 `note` 非空这一个信号，`type` 在 INSERT 之前就被丢掉了）；`cards` 表也没有 `updated_at`、`review_count`（真列叫 `reps`）。修法：`mapHighlight` 按 `note` 是否非空**推导** `type`（库里没有类型列这件事写进注释），`mapCard` 补 `state/stability/difficulty/reps/lapses` 并只留 `reps` 一个名字（不再 reps/reviewCount 两份口径），`mapBook` 只留 `progress`（`reading_progress` 是同一列的旧写法，7 处 `?? b.reading_progress` 是永远不成立的废话）。③ **同类横扫**：`Bookshelf` 的 `normalizeProgress(safeNum(b.progress ?? b.reading_progress))` 与两处 `c.nextReviewAt as string`、`Stats` 那段 `book as unknown as {…}`（里面还读着不存在的 `finishedAt/finished_at`）、`Chat` 把 IPC 行直接 `as unknown as BookRow[]` 再手写一遍 `String()/Number()`（映射器本来就在干这事）、`profileStore` 的 `reading_progress/is_finished/created_at` 裸列读 —— 全部改为走映射器。④ `tests/db-row-types.test.ts` 15 条**双向对账**：正向用真库 `PRAGMA table_info` 拿列名喂给映射器，要求「声明了的字段每个都造得出来、类型对得上」；反向扫映射器源码要求「每个字段至少有一个真实列来源，多余的别名算死代码也判红」，并**拿当初那五个假读法反证扫描不是空转**（负向断言配「该发生的确实发生了」）。⑤ 踩坑一条：第一版反向规则把「字段自己的驼峰名」一律放行，结果 `color: safeStr(row.color)` 因为名字等于字段而漏检 —— 放行必须只用于「已有真实来源时的兼容别名」，否则等于给假字段开后门。**门禁实测**：`npm run verify` 退出码 0（**1019 用例 / 60 文件**，eslint 0 error / 176 warning，typecheck 0 错误，三进程 build 成功）。**没验的**：界面一个都没点开过；「笔记页签从此有数」这句不能说满 —— 实测开发库 `%APPDATA%\zhixing-reader-dev\zhixing.db` 的 934 条划线里 `note` 非空的有 **0 条**（列名也逐个 `PRAGMA table_info(highlights)` 核过，确实没有 `type/color/chapter_uid`），所以这台机器上改前改后都显示 0，看不出差别：修的是「按不存在的列筛，所以就算有笔记也永远显示 0」这条断链，界面效果要等有真实笔记的书才看得出来。`DailyLearning/Vocabulary` 的生词与文章行没有映射器（另一条接缝）仍各自硬转，`import-weread-content` 仍读原始行（它要区分「章节名为 NULL」与「未知章节」，过映射器会被默认值抹掉）。| AI Agent（接手） |
| 2026-09-22（九续） | **ReviewStats 是一份六个字段全不存在的类型，顺带查出两份实现口径不同** —— ① `src/shared/types.ts` 的 `ReviewStats` 写的是 `totalCards / masteredCards / learningCards / newCards / averageEase / retentionRate`，而 `cardsDb.getReviewStats()` 真交出来的是 `{ total, due, new, learning, review }`：**一个都对不上**，`averageEase`/`retentionRate` 还是 SM-2 时代的概念（FSRS 换上来后早就不存在）。`renderer.d.ts` 把 `card.getStats` 标成这个谎类型，于是 `profileStore.ts` 只能写 `as unknown as Record<string, number>` 把类型绕过去 —— 界面没出错只因为读的人知道真相、绕开了类型。② **横扫同类抓到第二个真问题**：同一个 `getReviewStats()` 有**两份实现且 `due` 口径不同** —— `database/cards.ts` 带 `state != 0`（2026-09-15 那批修的），`repositories/card-repository.ts` 仍是老写法。同一份数据实测：cardsDb `{total:5, due:2, new:2}` vs 仓储 `{total:5, due:4, new:2}`（两张从未学过的划线被算成待办）。**用户画像服务 `analyzeCognitiveLevel()` 用的正是仓储那一份**，但它只读 `total/review/new/learning`、没读 `due` ⇒ 界面上当前没有错数字，这是颗等着踩的地雷。③ 修法：`ReviewStats` 改成真实五字段并把「due 只算已学过且到期」写进注释；三处声明（`cards.ts` / `card-repository.ts` / `types/repositories.ts`）统一引用同一份类型、不再各自内联形状；仓储那份的 `due` 补上 `state != 0`；`profileStore` 删掉绕类型的 cast。④ 测试 `tests/review-stats-type.test.ts` 6 条：真库里 seed 5 张卡（2 张新卡 due 已过 / 1 张学习态到期 / 1 张复习态到期 / 1 张复习态未到期），钉住「interface 字段集合 == 真实返回」「两份实现对同一份数据结果全等」「三处声明不再内联」「cast 不许回来」，红→绿。**踩坑两条**：(a) 我第一版把 `masteredCards` 也列进「虚构字段黑名单」，但它同时是 `LearningStats` 的合法派生字段（档案页在用）—— 黑名单会把「历史上写过这件事」当成「现在还在用」，收窄到 `averageEase`/`retentionRate` 两个真死字段；(b) 我先断言 `due === 1` 跑出来是 2，**是我的算术错**（学习态那张也过期了），期望值错就改期望值，不许改被测代码凑绿灯。**门禁实测**：`npm run verify` 退出码 0（**1004 用例 / 59 文件**，eslint 0 error / 176 warning，typecheck 0 错误，三进程 build 成功）。**没验的**：档案页与统计页没有点开看过 —— `card.getStats` 的运行时值本来就没变，这次动的是类型声明和仓储那份的 `due`。| AI Agent（接手） |
| 2026-09-22（八续） | **应用内「重启安装」弹「知行读书 无法关闭」的根因与加固** —— 用户完整走了「检查更新 → 下载更新 → 重启安装」，前两步在应用日志里都有（`Update status {"stage":"downloaded"...}`），第三步安装界面卡在重试框。**时间线是抢出来的**：`node_modules/electron-updater/out/BaseUpdater.js:13-27` 的 `quitAndInstall` 先**同步** spawn 安装进程，再把 `app.quit()` 排进 `setImmediate` —— 安装进程比主进程先起来；而 `node_modules/app-builder-lib/templates/nsis/include/allowOnlyOneInstallerInstance.nsh` 一开工就用 `tasklist /FI "IMAGENAME eq 知行读书.exe"` 找同名进程，`taskkill /im`（优雅）→ 循环 → `taskkill /f` 两轮后才弹 `$(appCannotBeClosed)`。**即主进程只要比这几百毫秒慢一步，就被当成「关不掉的程序」**。模板自带的注释写的是「App likely running with elevated permissions」—— 提权不匹配这条解释**没排除，也没证据**（本机没复现条件）。**加固（只改退出路径，不动安装器）**：① 新增 `electron/shutdown.ts` 的 `shutdownForExit()`，把原本散在 `before-quit`（和窗口 close）里的收尾收成一个幂等函数：`cancelActiveStream()` → `stopWereadAutoSync()` → `knowledgeCardService.shutdown()` → `closeDatabase()`（内部先 `forceSaveDatabase()`，同步 `writeFileSync`）→ `logger.close()`。② `updater.ts` 的 `quitAndInstall()` 改为 **spawn → shutdownForExit() → `app.exit(0)`**，不再把退出交给 `app.quit()`（`exit` 跳过 before-quit，所以落盘必须由我们自己做完，这是数据安全路径上的关键顺序）。③ `before-quit` 改调同一个 `shutdownForExit()` —— 两条退出路各写一套收尾，迟早有一条漏步骤。④ 新增守卫：`lastStatus.stage !== 'downloaded'` 时直接返回「尚未下载完成，暂时无法安装」，**spawn 之前拦住**，否则 electron-updater 内部只会 `dispatchError` 而我们已经把应用白关一次。**测试**：新增 `tests/install-exit-path.test.ts` 5 条（顺序固定 / 不许再出现 `app.quit()` 与 `setImmediate` / 未下载不得退出 / 库关闭排在日志之前 / 两条路共用同一收尾），红→绿验过。**门禁实测**：`npm run verify` 退出码 0（**998 用例 / 58 文件**，eslint 0 error / 176 warning，typecheck 0 错误，三进程 build 成功）。**没做的**：没为这条重新出包 —— **修复要随 v1.3.4 才到装机版，线上 1.3.3 仍是旧退出路径**；本机那次卡住的处理仍是人工通路（点「重试」或直接跑 `%LOCALAPPDATA%\zhixing-reader-updater\` 里已下载的安装包）；`app.exit(0)` 取代优雅 quit 之后**没有再实跑一次应用内更新**，安装器与主进程的实际赛跑只有推理、没有复测。| AI Agent（接手） |
| 2026-09-21（七续） | **装机版「继承了开发版的数据」根因查清并隔离** —— 用户把包装到 `测试专用/`，启动后发现书架/划线/API Key 全是开发期那套，没有全新安装的感觉。**证据链**（先量再判，没猜）：① 全盘只有一个 `%APPDATA%\zhixing-reader`（不存在 `知行读书` 目录），当天 21:42 那次启动的日志写着 `Database connected: C:\Users\Administrator\AppData\Roaming\zhixing-reader\zhixing.db`（1,531,904 字节），且**没有**「Auto updater disabled in dev」那行 —— 说明写这条日志的是装机版（`@electron/asar` 读包内 `package.json`：只有 `name: "zhixing-reader"`、`version: 1.3.1`，**没有 `productName` 字段**，`知行读书` 只用在 exe 名与卸载项）。② Electron 的 userData 取自 `app.getName()`，与安装位置无关 ⇒ 两边算出同一个目录。**结论：不是异常的 bug，是「数据目录跟着应用名、不跟着安装目录」的设计后果**（升级不丢数据靠的就是它）。**同类再扫一层发现更危险的**：`requestSingleInstanceLock()` 的锁也按 userData 上，所以开发版开着再点装机版，第二个进程会**静默 `app.quit()`**、只是把前一个窗口顶到前台（`main.ts:227` 注释自己写了「sql.js 不支持多实例并发写同一库」，而 sql.js 是整文件写回 ⇒ 两边同时跑真的会互相覆盖）。**修法（只改开发环境，装机版路径一个字不动）**：`main.ts` 在 `isDev` 时 `app.setPath('userData', appData/zhixing-reader-dev)`，且**必须排在申请单实例锁之前**（顺序反了锁仍然互斥）。新增 `tests/dev-userdata-isolation.test.ts` 3 条钉住「目录名不同 / 仅限非打包 / 排在锁之前」，红→绿验过；`npm run verify` 退出码 0（**993 用例 / 57 文件**，lint 0 error / 176 warning）。**本机善后**：先把 `%APPDATA%\zhixing-reader` 的 `zhixing.db`+`settings.json`+`Local Storage` 备份到 `%APPDATA%\zhixing-reader.bak-20260921`（1.5 MB），再把这份数据复制进 `-dev` 目录给开发版用，最后把装机版的三个数据文件移走 ⇒ 开发版保留原有书架、装机版从零开始。**没做的**：没为这条修改动重新出包（只影响非打包环境，用户侧无感）；装机版「重置数据库」这条 UI 通路没点过（改用的是移动文件，可逆）。| AI Agent（接手） |
| 2026-09-21（六续） | **更新失败提示收成一条 + v1.3.3 出包发布** —— 用户在装到 `测试专用/` 的应用里点「检查更新」，界面弹出 `更新失败: net::ERR_CONNECTION_RESET`。**先定性**：这不是代码缺陷，是本机到 GitHub 的网络不通（实测 `api.github.com` 200，但 `github.com` / `raw.githubusercontent.com` / 下载主机均 000，`latest.yml` 取不到）；**这条结论同时作废了上面几行「本机仍没装过任何 exe」的旧陈述**（用户已真装过，注册表有安装记录）。真正能修的是**提示本身两处**：① **同一次失败弹两条** —— 查 `node_modules/electron-updater/out/AppUpdater.js` 证实 `checkForUpdates` 是 `emit("error")` 之后又 `throw`（271-272 行），`downloadUpdate` 走 `dispatchError` 同理，于是「状态事件」和「按钮返回值」各报一次。改成**只认 `error` 事件这一路**，按钮拿到 `result.error` 只回滚状态不再弹；`applyUpdateStatus(status, fromCache)` 新增第二参，**「关于」页进页回读缓存时不再重放提示**（否则历史失败/成功会在新挂载时再弹一遍）② **错误码不该给用户看** —— 判定逻辑提成 `src/shared/update-notice.ts` 的 `describeUpdateError()`：网络类（`ERR_CONNECTION*`/`ECONN*`/`socket hang up`/超时…）收成一句「连不上更新服务器（GitHub）」，证书异常、访问频率受限、找不到安装包各给一句，其余保留原文便于排查。`tests/update-notice.test.ts` 6→11 条（含「不得出现 `net::` 与错误码」的负向断言）。**顺手**：「设置 → 关于」的更新历史改为短句分条、一条只说一件事；去掉 '• ' 手打前缀后圆点会全丢（**Tailwind v4 preflight 把 `ul` 的 `list-style` 清成 none**），靠内联 `listStyleType: 'disc'` 写回来。**发版**：走六处同步点，`npm run verify` 退出码 0（990 用例 / 56 文件，lint 0 error / 176 warning），`installer/zhixing-reader-Setup-1.3.3.exe` 119,246,454 字节（比 1.3.2 少 850 字节），随包 exe FileVersion 1.3.3、`latest.yml` 的 version/size/sha512 与本机 exe 逐字节自洽、`dictionary.json` 随包字节与源文件相同。`.gitignore` 加 `测试专用/`（试装树 465 MB 不入库，没删用户安装）。**已发布**：tag `v1.3.3` 已推、Release 标为 Latest、三件同传（服务端报 exe 119,246,454 与本机一致），并从线上回下载对账——`gh release download` 与直连 `github.com/.../releases/download/v1.3.3/latest.yml` 各取一次，两份都是 357 字节且与本机产物**逐字节相同**（`curl` 走通这点本身是个新信息：上面那条 `ERR_CONNECTION_RESET` 至少在此刻不再复现，顶栏「新版本 v1.3.3」这条链路下次装 1.3.x 时可以直接试）。**仍没验的**：应用内点「检查更新 → 下载更新 → 重启安装」的完整一遭，仍没人肉眼走过。| AI Agent（接手） |
| 2026-09-21（五续） | **v1.3.2 出包并发布** —— 把上一批统计页口径修复打出去。走 `tests/version-sync.test.ts` 的六处同步点（`npm version` / `APP_META` / CHANGELOG `## [1.3.2]` + 链接行 / README 五处 / 关于页 `UPDATE_HISTORY` 首条），`npm run verify` 退出码 0（985 用例 / 56 文件，lint 0 error / 176 warning）。**产物实测**：`installer/zhixing-reader-Setup-1.3.2.exe` 119,247,304 字节（113.7 MiB，比 1.3.1 只 +619），`win-unpacked/知行读书.exe` FileVersion 1.3.2，`latest.yml` 的 `version: 1.3.2` + `size` 与 exe 一致、sha512 与本机 exe 逐字节自洽，`dictionary.json` 随包字节与源文件相同。**已发布**：tag `v1.3.2` 已推、Release 标为 Latest、三件同传并从线上回下载对账。**这一版顺带是那条更新提示链路的第一次真验机会**：装 1.3.1 的应用启动后应能在顶栏看到「新版本 v1.3.2」——但**本机仍没装过任何 exe**（注册表无安装记录），所以这条链路依然是代码推理，没人肉眼看过。| AI Agent（接手） |
| 2026-09-21（四续） | **统计页三处假口径 + 恒为 0 的「费用」列**（commit 12896a9，已随 v1.3.2 发布）—— 承接 09-20 清单里剩下的候选。① **趋势图**：chip 上写「近 7 天 / 近 30 天 / 近 12 个月」，可微信读书 `mode=weekly/monthly/annually` 给的是**本周 / 本月 / 本年**窗口，而 `ReadingTrendBars` 自己又切一次 —— `showCount = annually ? 12 : 7` ⇒ 选「近 30 天」只画**最后 7 根**、选「本年」把 12 个**按天**的点直接标成「1月…12月」。修法：口径收进 `src/shared/reading-trend.ts`（`READING_TREND_SPECS` 一处定 chip 名 / 图标题 / 徽标 / 分桶粒度），`buildReadingTrendPoints` 按月聚合走本地月份（跨年不把两个 3 月并一个）；chip 与 `modeLabels` 都从同一份 spec 生成，**名字只有一套**，7 条测试钉住「给 30 天必须画 30 根」 ② **热力图整列错位**：取数窗口和格子都用 `toISOString().split('T')[0]`（UTC 日），UTC+8 凌晨把「今天」算成昨天 ⇒ 今天那一格永远空 —— 新增 `recentDayKeys()` 本地日期串，取数 / 格子 / 徽标三处共用（同 09-20 档案页那个 off-by-one 的同类） ③ **两张卡的徽标挂错数**：KPI「复习卡片」显示的是全库卡片总数（`bookStats.reduce(cardCount)`，含从没复习过的）却摆在时段卡旁边；「复习热力 · 近 12 周密度」的徽标也挂总数 —— 改成标题如实写「卡片总数 / 由划线自动建卡，累计至今」，热力徽标改成**近 12 周实际复习次数**（与格子同一批 key 求和）④ **`cost_usd` 一列恒 ¥0.00**：`token_usage.cost_usd` 有列、`DEFAULT 0`，全仓库**没有任何写入方**（只有 entities 类型 + 渲染层读），旁边还留着 `USD_TO_CNY = 7` 的注释「与设计稿 ¥18.60 量级一致」—— 一个为了让设计稿看起来对而定的汇率。砍掉请求日志的费用列 + `formatCost` + `USD_TO_CNY` + KPI 里没地方用的 `totalCostUsd`；CSV 仍按数据库原样导出该列（那是真列，不是界面承诺）。`WeeklyBars` 顺手改名 `HourlyBars`（它一直画的是 24 小时分布，名字与注释都在说"一周/7 日"）。测试 974→985（55→56 文件），typecheck 0 / eslint 0 error。**没验的**：统计页与 Token 页**没有逐个点过**（`npm run dev` 需要微信读书数据才有图，本轮只做 typecheck/eslint/单测/build）| AI Agent（接手） |
| 2026-09-21（再续） | **v1.3.1 出包** —— 把上面那批「检查到更新却没人知道」的修复打成可安装版本。走 `tests/version-sync.test.ts` 钉住的六处同步点（`npm version` / `APP_META` / CHANGELOG `## [1.3.1]` + 链接行 / README 五处 / 关于页 `UPDATE_HISTORY` 首条），一次改完全绿才算发版。**产物实测**：`installer/zhixing-reader-Setup-1.3.1.exe` 119,246,685 字节（113.7 MiB，比 1.3.0 只 +909 字节），`win-unpacked/知行读书.exe` FileVersion 1.3.1，随包 `latest.yml` 的 `version: 1.3.1` + `size` 与 exe 逐字节对上、sha512 与本机 exe 实测一致，`dictionary.json` 随包字节与源文件相同。`npm run verify` 退出码 0（974 用例 / 55 文件）。**仍未做**：没跑安装包装一遍（这台机器注册表里没有已安装记录，从没在真安装态下点过）。**已发布（用户说「该推送的都推送」）**：tag `v1.3.1` 已推，Release 标为 Latest，三件同传后从线上回下载对账 —— `latest.yml` 与本机产物**逐字节相同**、`.blockmap` 字节数相同、exe 服务端报的 size 119,246,685 与本机一致 | AI Agent（接手） |
| 2026-09-21（续） | **自动更新的「检查了但没人知道」修掉** —— 用户问「应用内会自动检查更新吗、会自己装吗、要跳 GitHub 手动下吗」，逐个查代码后发现：**打包版启动确实静默查一次**（`electron/updater.ts`），下载与安装都留在应用内（`autoDownload=false` + `autoInstallOnAppQuit=true` + `quitAndInstall(false,true)`；更新态安装器带 `--updated` 且 `skipPageIfUpdated` 跳过目录页、从注册表 `InstallLocation` 覆盖原目录，数据在 `%APPDATA%` 不受影响，**不跳 GitHub**，只有 dev 环境才降级成「API 比版本 + 打开下载页」）。但那条 `UPDATE:STATUS` 是**单向推送、错过不补**，而全应用**唯一的订阅者是「设置 → 关于」**——启动检查时用户在首页，结果永久丢弃，界面表现为「永远没有新版本」。三条修法：① `electron/updater.ts` 缓存 `lastStatus` + 新增 `UPDATE.GET_STATUS`（通道 163→164，preload/renderer.d.ts 同步）② **每 6 小时重查**（`UPDATE_RECHECK_MS`，`before-quit` 里 `stopUpdateChecks()` 清定时器；下载中/已下载不重查）—— 应用常连着开几天，只查一次等于没有 ③ **顶栏通知面板**加「新版本 vX」一条（点 → `/settings/about`），「关于」页进页先回读缓存、没有结果才自动查（**dev 不自动查**，因为那条降级通路会直接打开浏览器）。判定逻辑提成 `src/shared/update-notice.ts`：只有 `available`/`downloaded` 提示，其余一律 null —— **「已是最新」必须把旧提示收回**，否则红点会永远挂着。6 条测试钉住这点。**同类已横扫**：`onPersistError`/`onSyncBookshelf`/`onWereadAutoSyncStatus`/`onNavigate` 的订阅者都在 `App.tsx`/`Topbar.tsx`（常驻），只有更新状态这一条是晚挂载页面独占 —— 现在它也由顶栏订阅了。**未实测**：`npm run dev` 里 `app.isPackaged=false`，整条更新链路走不到，所以这次改动只过了 typecheck/eslint(0 error)/974 单测/build，**打包环境一次没点过**；要真验，得装一次 exe 再 Release 一个更高版本。测试 968→974（54→55 文件）| AI Agent（接手） |
| 2026-09-21 | **v1.3.0 发版收尾** —— 版本号从 1.2.0 提到 **1.3.0**（v1.2.0 之后累计 38 个提交、含 8 个 `feat`，按 semver 走 MINOR）。发版时把**散落的版本字符串一次对齐**：`package.json` + `package-lock.json`（`npm version --no-git-tag-version`）、`src/shared/external-links.ts` 的 `APP_META.version/releaseDate`、`CHANGELOG.md`（新增 `## [1.3.0]` 与链接行）、`README.md`（顶部横幅、Version 徽标、安装包文件名、变更记录表、页脚）、关于页 `UPDATE_HISTORY` 首条。**顺手把这四处手工同步点钉进** `tests/version-sync.test.ts`（原来只钉 APP_META 与 CHANGELOG 两处，README 与关于页此前就漂移过两次）—— 漏改任一处测试直接判红。文档口径同步：README 的测试数 885/42 → **968/54**、AGENTS 966 → **968**（实跑确认）、表数 15 → **16**（`schema.ts` 实测 16 个 `CREATE TABLE`，`resetDatabase` 清理列表同为 16）。`npm run verify` 退出码 0（968 用例全绿，lint 0 error）。**安装包已出**：`npm run package:win` → `installer/zhixing-reader-Setup-1.3.0.exe`（113.7 MB / 119,245,776 字节），产物内 `win-unpacked/知行读书.exe` 的 FileVersion 实测 1.3.0，随包 `resources/latest.yml`（version 1.3.0）的 sha512 与 exe 实测自洽（自动更新按它校验），`extraResources` 的 dictionary.json 字节数与源文件一致；README 里写死的「125MB」按实测改成 113.7 MB。**未做**：没有启动安装包/打包后的 exe 做端到端冒烟。**发布（同日晚，用户批准后）**：tag `v1.3.0` 已推，GitHub Release 三件同传（exe / `.blockmap` / `latest.yml`），线上 `latest.yml` 实测 `version: 1.3.0` 且 sha512 与本机 exe 逐字节一致 | AI Agent（接手） |
| 2026-09-20（续） | **前端假数字 / 断线字段清扫**（commit 414101b、bb07384 与本批）—— 起因是用户指着档案页的「年度阅读 0 小时」说「把假的、不通的前端都修复完善一下」。立了一条口径：**界面上每个数字必须能追到真实数据库列，追不到的宁可删掉也不编**。① 档案页：算法全部提成 `src/shared/profile-stats.ts`（窗口/连续/热力/趋势/时长人话），`profileStore` 一次 `Promise.all` 取「今年至今 ∪ 26 周」窗口，`LearningStats` 重写（新增 reviewedCards / lastReviewAt / dailyRows / firstRecordAt，删掉没人用的 getWeekly/getMonthly），热力图不再单独发 IPC 而由同一批 dailyRows 派生 —— 顺手修了 `toISOString()` 的 UTC  off-by-one ② **删掉「加入知行 N 天」的编造值**：`DEFAULT_PROFILE.joinedAt` 改成空串并注释禁止「now − N 天」，显示时回退到「第一条真实记录」（实测库里最早是 `2026-09-02 12:26:13` → 界面显示 `2026-09-02 开始记录`）；用户 settings.json 里那条假的 `userJoinedAt` 已按授权删除（备份 `settings.json.bak-20260920`）③ **两处真断线**：`admin.getCardsByBook` JOIN 了不存在的 `knowledge_cards.highlight_id`（一进后台就抛错，界面显示"暂无数据"）、`cards` 查询丢 `book_id`（卡片无法归属到书）—— 都补上并各写测试 ④ **复习 CSV 六列里五列常年空白**：读的是 SM-2 时代的 `cardId/quality/easeFactor/interval/reviewedAt`，而 reviews 真实列是 `card_id/rating/review_time/elapsed_days/scheduled_days`；改成 `src/shared/review-export.ts` 的 `REVIEW_CSV_COLUMNS`（表头和取数同一份 spec），测试拿真实 schema 的 `PRAGMA table_info(reviews)` 双向对账 ⑤ **顶栏「100 张卡片待复习」是假的**：用 `card.getDue(100).length` 当计数，卡片堆到几百张时永远显示 100；改走 `getQueueStats().actionable` ⑥ **两个假控件/假分母直接砍**：设置页「难度衰减」（引擎从不接受该值，FSRS-6.0 的 decay 是常数 0.1542）和存储条的 `/ 512 MB` 上限（应用没有容量上限这个概念），连带修掉「可在清理缓存下查看路径」这种做不到的指引；`Review` 类型换成真实的 `ReviewRow`（snake_case）⑦ 新增 `tests/no-fake-controls.test.ts` 用源码扫描钉住这些写法不许回来（同 design-tokens / brand-assets 的路子）。测试 917→966（50→54 文件）。**同批再清三处**：知识卡片「复习 0 次」（review_count 建了列但没有写入方，FSRS 复习的是划线卡片不是知识卡片）、每日学习拿被默认 limit 截断的列表长度当计数（本机实测 80 篇文章 > 默认 50，会把「还有没读完的」算成「都读完了」）、策略表写死的「4 条映射」（改 `INTENT_META.length`）。**核实后判定是误报的两条**：`YearlyBookTable` 的「100%」（该表只列读完的书，本来就是 100%）与设置页「自动模式」徽章（Bloom 升降级在 orchestrator 里是真实现）。`npm run verify` 退出码 0。**踩坑两条**：(a) 源码扫描类测试会咬到自己 —— 我在被扫文件里写的注释带了被禁的字符串，测试直接判红，注释得绕开字面量；(b) `src/renderer/**/settings/*` 引 `src/shared` 是四层 `../../../../shared/`，照抄页面的三层会解析失败。**界面只过了 typecheck/build/单测，没有逐个点过** | AI Agent（接手） |
| 2026-09-20 | **摘要「只报不烧」**（commit 4179e5f）—— 划线改了之后 AI 摘要就过期，但界面上没有任何地方提醒，用户要么自己翻到书籍详情才发现，要么永远用着旧摘要。收口时立了一条边界：**启动/通知只做纯本地计算，一律不调 AI**（自动烧钱需要单独授权）。新增 `SUMMARIES.FRESHNESS`（通道 162→163）：`highlightsDb.getChapterCounts()` 一次 SQL 汇总 + `chapterSummariesDb.getAllSourceCounts()` → `src/shared/chapter-summaries.ts` 的 `findPendingSummaryBooks()` 纯判定（**与生成侧 `planChapterSummaries` 同一套分章口径**，空章节名归到 `UNGROUPED_CHAPTER`，划线数为 0 的章不计），按「欠更新的章数」倒序最多报 6 本。顶栏通知面板列「N 本书划线有变化，摘要待更新」，点某本 → `/bookshelf/{id}?tab=summary`（BookDetail 初始页签改为可读 `?tab=`），仍要用户自己按「生成 AI 摘要」才花钱。**踩坑一条**：SQL 汇总和内存分章是两条实现，口径漂移会静默变成「该报的不报」—— 项目里这类列名/口径漂移已咬过两次（`BookSummary` 的 `content` vs 真实 `summary`、934 条划线缺章节名），所以 `tests/summary-freshness.test.ts` 直接钉住**两者对同一份数据结果全等**，而不是只测其中一条。测试 901→917（50 文件），`npm run verify` 退出码 0。**界面部分只过了 typecheck/build/单测，没有实际点过** | AI Agent（接手） |
| 2026-09-19（第四续） | **B1 第二批：6 个非流式函数迁到 AI SDK 通路**（commit 7cbb090 / f5eeda4）—— 章节摘要、全书摘要、卡片解读、卡片应用、Skill 导出、文章翻译全部从手写 `callAI` 改走 `sdkGenerateText` / `sdkGenerateObject`。**为什么这六个能走**：它们的输出形状要么是一段纯文本、要么是一个 `{summary, keyPoints}` —— 换 zod 严格 schema 的代价（不合规从「`repairJSON` 修修能用」变「抛错」）只在**模型自由输出 JSON 数组**的那两个函数上才真正刺眼，那两条留到人在电脑前真打 API 时做。**三处收口**：① `buildMessages` 从 ai-service 私有函数提成 `electron/services/prompt-messages.ts`，两条通路共用同一套模板（否则迁移期就是第三处双写）② 非流式调用统一下发 `reasoningEffort: 'none'`（沿用流式那边实测的教训：思考型模型会把输出预算先烧在 reasoning 上，小预算直接空正文）③ 用量按 `feature` 逐次落库，含 `cachedInputTokens`。**顺手修一个排版坑**：Skill 的 `{{nameEn}}` 在模板里顶在「触发场景:」前面，旧写法把**裸英文名**塞进去 → 渲染成 `Pomodoro触发场景: …`；纯中文名 slug 化后是空串，旧代码回退成 `methodology` 等于**编造名字**。现在提成 `src/shared/skill-name.ts`：整行自带换行、没有英文名就整行省略。**踩坑两条**：(a) 搬运测试时把载体从 `generateChapterSummary` 换成 `extractMethodologies`，那条 `max_tokens=4000` 断言量的已经不是同一个东西（抽取调用显式给 8000）—— 换载体时必须逐条重读断言，失去意义的就删掉并说明原因；(b) 用脚本按大括号计数删 `describe` 块，**字符串字面量里的 `{` `}` 会把计数带偏**，留下三行孤立 `)` 让整份测试无法 transform（**`npm run typecheck` 抓不到**：根 `tsconfig.json` 的 `include` 只有 `electron` / `src` / `scripts`，测试文件不在其中，只有真跑 vitest 才暴露）。删块用「行号 + 该行内容断言」而不是数括号。结果：ai-service 1132→873 行，测试 892→901（SDK 侧新写 30 条含红绿验证），`npm run verify` 退出码 0 | AI Agent（接手） |
| 2026-09-19（再续） | **VIS 第三轮：字标转曲 + 调色板单一真值** —— ① **字标**：思源黑体 SemiBold(600) 描骨后用 `opentype.js@2.0.0`（MIT）转曲成 `brand/wordmark.svg`（+0.04em 字距，墨迹框 `22 -847 4058 940`，`currentColor`），另出错落版 `brand/logo-horizontal.svg`（viewBox `0 0 164 48`：字标高 24 = 环外径 38 的 63%、间距 12 = 徽标箱 25%）。**为什么必须转曲**：SVG 里的 `<text>` 吃本机系统字体，换机器/CI 就变样（上一轮实测过）。字重选 600 是因为它等于 UI 的 `--font-weight-semibold`，字标和界面"一种口气"。**UI 里不用字标** —— 侧栏/关于页仍是真文本，可选可译可读屏。组合结果用像素矩阵量过：中心线偏差 **0.0px**、间距区着墨 **0.00%** ② **调色板收口**：新增 `tokens/brand.json`（DTCG）作全部色值唯一真值，`scripts/build-tokens.mjs` 生成 `styles/generated-palette.css` + `design/palette.ts`，`design-tokens.css` 的语义 token 全部改成 `var(--emerald-600)` 这类引用、**裸 hex 清零**，`colors.ts` 改为消费 palette；带 `--check` 模式，测试跑它判产物是否过期 ③ **顺手扩范围**：grep 发现"双写"远不止两处 —— admin-charts.tsx（5 处）、token-usage/constants.ts（2 处）、Topbar 的 `var(--chart-1, #10b981)` 兜底、Badge 注释都夹带裸 hex，一并改为引用；全仓库 hex 统一小写 ④ 验证：打包后入口 CSS 同时含 `--emerald-600` 定义与 `--primary:var(--emerald-600)`，var 链闭合（Tailwind 工具类走同一条链）。**踩坑两条**：测试自己写的断言反被绊倒 —— (a) 把 `white:#ffffff` 纳入"禁止裸 hex"导致误报，禁止范围必须限定 emerald+brand 两组；(b) SVG 用大写 hex 而 JSON 小写，大小写不一致会静默漏检，已全库统一小写。新增 `tests/design-tokens.test.ts` 7 条，测试 885→892（49 文件），`npm run verify` 全绿 | AI Agent（接手） |
| 2026-09-19（续） | **VIS 第二轮：字体本地化 + 中文字体栈补齐** —— 上一轮查出的两个遗留一起修：① `--font-sans` 只有 `"DM Sans"`（**无中文字形**），四个中文字一直在吃 Windows 默认回退，字体气质不可控；② 字体从 `fonts.googleapis.com` 拉（**桌面应用离线打不开、国内常被墙**）。现在三款字体全部本地打包：`@fontsource-variable/{noto-sans-sc,dm-sans,jetbrains-mono}`（**OFL-1.1，可变字重 100–900 一个文件**，devDependency，woff2 由 Vite 打进 `dist/renderer/assets`，实测 105 个 woff2 / 4.7MB / 最大分片 76KB，不进 git）。字体栈顺序是 **拉丁在前、中文在后**（`"DM Sans Variable", "Noto Sans SC Variable", "Microsoft YaHei UI", "PingFang SC", ui-sans-serif`）—— 反过来会让英文和数字换一套字形。OFL 义务：三份许可文本（含 `Copyright` 行）放进 `src/renderer/public/licenses/` 随包分发，关于页开源许可表加三行。**顺手删掉 `--font-serif`（定义了但全项目零消费的死 token）**。新增 `tests/typography-assets.test.ts` 8 条：无 CDN、依赖声明与入口引入、字体栈含中文族、拉丁族在前、许可文本存在且含版权行、关于页列出的字体数 = 3、死 token 不许回来。打包实测 `dist/renderer` 无 `fonts.googleapis.com`、CSS 里是 `url(./…woff2)` 相对路径（`file://` 下可用）、4 个 <4KB 子集被 Vite 内联为 base64（正常）。测试 877→885（48 文件） | AI Agent（接手） |
| 2026-09-19 | **品牌 VIS 第一轮 —— 徽标「玉璧」+ 图标生成流水线**（commit 5146f8e / bab6629）—— 起因是项目里躺着 **4 份互相打架的 logo**：`resources/logo.svg` 的渐变书只有官网在用、侧栏与关于页是 CSS 手写的绿方块「知」字占位、favicon 还是 Google 蓝 `#4285f4`。先做开源方案调研（结论：可商用的 text-to-SVG 模型输出的是重建 path 不是可维护几何；FLUX.1-dev / FLUX.2-klein-9B / Ideogram 4 / IconShop 全部禁商用或根本没 LICENSE；方正/汉仪做 logo 需单独授权），再落两套稿。**第一版「拾级」（台阶剪影）被用户否掉**——量了原因：平涂 emerald 满铺色块着墨率 **84%**，天生像系统默认图标。第二版「玉璧」：r=16 / 线宽 6 的环开 60° 口，缺口正中嵌 6×6 铜金方（知行合一，缺的那一步由「行」补上），着墨率降到 **23.3%**。**一套几何四种配色**（mark / mark-reverse / mark-icon / mark-mono），环与方的坐标在六处派生物里逐字相同，`tests/brand-assets.test.ts` 15 条钉住（含「方中心落在 −30° 角平分线、到圆心 16.12≈半径」的几何自洽断言）。三条实测踩坑：① **electron-builder 的 `app-builder icon` 不接受 SVG**（`icons.LoadImage` 直接报错），且从 PNG 只产出 256 一档 → ICO 必须自己写容器（`scripts/build-icons.mjs` + `npm run build:icons`，16/20/24/32/40/64/96/128/256 十档 PNG 压缩条目）；② **SVG 里的中文 `<text>` 吃本机系统字体**，换机器/CI 结果就变 → 字标将来必须转曲；③ **应用图标必须带底板**，无板的墨绿环贴深色壁纸会直接消失（品牌徽标不带板，两者共用同一组坐标）。配色立了新规：**品牌墨与 UI 交互色是两个口径** —— 新增 `--brand-ink #0c3b2e` / `--brand-brass #b08d57` / `--brand-paper #f5f1e8` / `--brand-mark`（暗档自动反白），`--primary` 仍是 emerald-600 一行未动。对比度全量实测：墨绿对白 12.50、宣纸对墨绿板 11.08、铜金对墨绿板 4.04、**铜金对白只有 3.09 → 铜金只做点缀不承载信息**。测试 862→877（46→47 文件）。**VIS 遗留**：字标（OFL 字体描骨后转曲，不能用字库直出）、`tokens/brand.json` 收掉 design-tokens.css 与 colors.ts 的双写 hex、**中文字体从未指定**（`--font-sans` 只有 DM Sans，四个中文字一直在吃 Windows 默认回退）且字体走 Google Fonts CDN（离线/墙内首屏会掉） | AI Agent（接手） |
| 2026-09-18（再续） | **B1 前置：先把死链砍干净，再谈迁移** —— 用脚本扫 preload 暴露面，查出 **27 个渲染层零消费者的方法**，逐个核实「主进程内部还有没有别的调用方」后砍掉 26 条真死链（通道 + handler + preload + renderer.d.ts 类型）：① 老通路整条手写流式实现（ai.streamChat + agent:streamChat + streamOpenAI(复杂度46) + streamAnthropic(40) + legacy cancelActiveStream）—— 渲染层走的是 STREAM_CHAT_WITH_CONTEXT，手写 SSE 那半壁早已被 AI SDK 取代且更完整；② 四个被界面弃用或被新功能取代的非流式函数（generateCards / generateSummary / chatWithContext(早标 @deprecated) / explainHighlight）+ skill.exportBatch + 8 个提示词模板（注册表 30→22）；③ 20 条数据读取死链（books:updateProgress、cards:getByHighlight/createBatch、reviews:getByCard、articles:getUnread/getFavorites、vocabulary:getByWord/incrementReview、dictionary:getSize、weread:fetchBookmarks/fetchNotes、readingData:fetch{Weekly,Monthly,Annually,Overall}、knowledgeCards:getByType/isDistilling、admin:getPrompt、fsrs:getForecast/getOptimalReviewOrder、preload stats:getWeekly 包装）。**保留了链路断了但实现仍活的**：cardsDb.getByHighlightId（单条建划线时防重复建卡在用）、vocabularyDb.getByWord、reviewsDb.getByCardId、weread-api 的 fetchBookmarks/fetchNotes（fetchAllContent 内部调用）、dictionaryService.getSize、knowledgeCardService.isDistilling（有测试）。顺手修一个测量缺陷：老通路解析响应时丢了 prompt_tokens_details.cached_tokens，导致统计页缓存命中率对这 8 个功能恒为 0（2 条新用例，红绿都验过）。结果：通道 188→162，ai-service 1664→1132 行，preload 541→521，fsrs-engine −42，测试 928→862（删的全是被砍功能自己的用例）。**踩坑一条**：判断「实现是否已成孤儿」时，grep 若把 electron/ipc/ 整个排除掉，就会把仍在 handler 里用的 cardsDb.getByHighlightId 误判成死的 —— 排除范围只能到「本次正要删的那个 handler」。B1 剩余：8 个仍活着的非流式函数迁 AI SDK（换成 generateObject + zod 后，模型不合财会从「repairJSON 修修能用」变成「抛错」，必须逐条真打 API 看形状，适合有人在电脑前时做）。commit 6a88bdb / 93ddc41 / f221c8f / 66838bc | AI Agent（接手） |
| 2026-09-18（续） | **巨型页拆分第二、三批（纯搬运，零逻辑改动）** —— DailyLearning 2190→1622（`pages/daily-learning/` ×4）、VocabularyPage 1972→1091（`pages/vocabulary/` ×5）、KnowledgeCards 1789→1058（`pages/knowledge-cards/` ×4）、Methodologies 1787→1004（`pages/methodologies/` ×5）。搬运块用脚本比对确认与原文件逐字相同，页面本体只换 import。**结论**：能整块搬走的顶层子组件已基本搬完，剩下 10 个 >1000 行的文件都是单个巨型组件本体（状态与 JSX 互相引用几十处），再拆需要能真点一遍 UI 的会话，不靠盲改。踩坑两条：enum 不在常规的 export 前缀清单里（ReviewRating 枚举漏迁一次）；删区间时容易连带删掉组件签名那行本身（typecheck 立刻抓到）。commit 4c30af7 / 5d21a38 / 42b477f / 613af17 | AI Agent（接手） |
| 2026-09-18 | **一次性清空六批债务（commitlint / Modal 收口 / 死代码 / 模型分档 / 层级摘要 / 巨型页拆分）** —— ① `.husky/commit-msg` + `commitlint.config.js`（config-conventional，header ≤120，`subject-case` 关闭放行中文），此前 R10 只靠人工 ② VocabularyPage 最后两处手写弹层迁 `ui/Modal`（抽屉生词详情 + 导出对话框），全应用 6 处弹层收口完毕 ③ `types/repositories.ts` 5 个无实现接口 + `sqlite3.d.ts` 删；`build.files` 白名单 84→50 项（只留 node_modules 里确实没有的）；preload 监听器改造前先核实：**12 个调用点全部成对注销，改造零收益**，改为把「必须持有返回的清理函数」写进注释 ④ **主线 A Step 6 模型分级路由**：`src/shared/model-routing.ts`（`resolveChatTier`，白名单意图 casual_chat 走经济档）+ 设置项 `llmModelFast` + orchestrator 传 intent，未配置时恒走主模型 ⑤ **主线 A Step 5 书籍层级摘要（RAPTOR 简化）**：新增 `chapter_summaries` 表（第 16 张）+ `chapterSummariesDb.upsertBatch`（sql.js 落盘是全库导出，一次生成只导盘一次而不是 N 次）+ `src/shared/chapter-summaries.ts` 纯判定（分章、按「该章划线条数」判新鲜度、注入文案必须标明是 AI 概括）+ `chapter-summary-service`（L1 逐章 → L2 由 L1 汇总；L1 没变就不重烧 L2；同书并发直接报错，防双份 AI 花费）+ 4 个提示词模板（注册表 26→30）+ IPC `SUMMARIES.GET_CHAPTERS/GENERATE` 全链路 + BookDetail 新增「摘要」页签与「生成 AI 摘要」入口 + `book-context-builder` 在关联书籍时注入全书摘要与 BM25 挑出的 3 章摘要。**顺带查出**：`book_summaries` 表和它的 IPC/preload 一直全在，但**渲染层零引用**（第 6 个断链），且 `BookSummary` 类型写的是 `content/createdAt` 而真实列是 `summary/generated_at` —— 现在两头对齐并真在界面上显示 ⑥ 巨型页第一批纯搬运：Stats 2993→553（`pages/stats/` ×7）、TokenUsage 1612→974（`pages/token-usage/` ×6）、SettingsData 1541→1185（`data-utils.ts` + `use-data-io.ts`），逻辑一行未改。测试 895→928（44→47 文件），`npm run verify` 退出码 0（lint 0 error / 202 存量 warning），已推送 | AI Agent（接手） |
| 2026-09-16 | **默认对话路径的「零上下文」修复 —— 检索第一次在真实使用中生效** —— 上一轮把中文切词修好了，但**默认路径根本不会调用它**：`book` / `knowledgeCard` / `methodology` 三个构建器的 `shouldBuild` 都要求 `!!context.bookId`，而从首页进入「AI 对话」时是不选书的（对话框上的「关联书籍」是一个可选的虚框按钮）。在**打包后的应用**上实测（CDP 发一条真实提问）：「调取知识库」只有 **2/2 路**（相关记忆 + 用户画像），`promptTokens` 仅 **358**，AI 回答「你提供的笔记里并没有直接出现…」—— 934 条划线一条都没进提示词。修法：三个构建器不再以「有没有选书」为门槛 —— 选了书只搜那本书（用户显式意图），没选书就跨全部书籍 / 全部卡片 / 全部方法论检索，并在注入文本里写明「来自你的全部书籍，与当前问题无关就忽略」。同时修掉三个同源缺陷：① 卡片与方法论的相关性打分原来是拿**用户整句**去 `content.includes(...)`（中文没有空格 → 几乎永远为 false），于是「相关卡片」实际是按数据库顺序硬塞 10 张、分数全是 0；现在复用 `src/shared/retrieval.ts` 的 BM25 + 中文 bigram，只注入真正命中的，一条都没命中就不注入（面板如实显示「无命中」）② `searchIndex` 的噪声词过滤只看出现比例，1~2 篇的小语料里**唯一的信号会被当成噪声整段丢掉**（实测知识卡片命中数恒为 0），加了下限 `df >= 3`（真实 934 条划线语料的行为完全不变）③ 卡片类型读的是不存在的字段 `card_type`（列名是 `type`），所以「【卡名】(类型)」里的类型从来没显示过。**打包实测（用户真实数据，一次真实提问）**：934 条划线建索引 →「作者怎么看人际关系」命中 **5 条 / topScore 17.2**，方法论 **1/11**、知识卡片 **5/90**，`promptTokens` **358 → 1252**，回复末尾出现「**引用来源：5 个片段**」。新增 5 条测试（含「没选书也要检索」「不相关就不注入」）。测试 880→885（42 文件） | AI Agent（接手） |
| 2026-09-16 | **按计划执行 15 个任务（轻量 RAG + 交互审计遗留全清）** —— 完整计划见 `docs/superpowers/plans/2026-09-16-lightweight-rag-and-fixes.md`（6 个阶段 / 15 个任务 / TDD）。核心是**用本地词法检索替换掉从未生效的向量语义检索**：① 新增 `src/shared/retrieval.ts`（BM25 + 倒排索引 + 中文 2 字滑窗，零依赖零网络）；② `rag-service` 退化为「取数 → 按签名缓存索引 → 调纯函数」的适配层，签名含**写计数器**（`updated_at` 只到秒，同秒内的导入+回填不会触发重建）；③ 删除 Vectra + embedding-service + `vectra` 依赖（少约 50 个传递依赖）。**实测验收（用户真实 934 条划线，零 AI 调用）**：「作者怎么看人际关系」命中《被讨厌的勇气》「第二夜 一切烦恼都来自人际关系」；修复前同四个问题命中 **0 条**。其余 12 个任务：生词本「加入复习」不再偷记一次评分、评分防连点、导出改为导出全部；统计页两套时间范围各自说清、书单行真能点；Token 页 KPI 口径不再被列表筛选截断；备份导出补全知识卡片/方法论/生词且导入真的恢复（含复习卡片按划线重建）；删除 4 个无人消费的模板开关；KPI 卡片要么真能点要么不装；每日学习筛选为空不再假装显示全部、翻页下标统一、复习可连续；空状态补出口、提示词中心四处；后台搜索/筛选范围如实标注；**砍掉 book_architecture 全链路（-299 行）**；系统加密不可用时如实告知密钥为明文。测试 888→880（42 文件，删掉 13 条测试死代码用例、新增 22 条检索用例） | AI Agent（接手） |
| 2026-09-16 | **AI 检索链路修复：中文提问原来是"零上下文"** —— 顺着「向量索引目录只有 79 字节」查下去，发现三个叠加的断点：① **中文切词是错的**：`keywordSearch` 用 `query.split(/[\s,，。？?！!、]+/)` 切词，而**中文句子没有空格**，整句会变成一个"词"（如「作者认为人际关系重要吗」），拿去 `includes` 几乎永远为 false —— 实测用真实数据库对比：**四个日常中文问题，旧切词命中 0 条划线；改成 2 字滑窗 bigram 后命中 33~79 条**。② **空索引被判成"可用"**：`checkRAGAvailability` 只看索引文件能不能打开，不看里面有没有向量（实测 0 条），于是走语义检索拿到空结果；③ **空结果不回退**：语义检索返回 0 条时 builder 直接把空数组返回，不落回关键词检索 —— AI 就带着零条书籍上下文回答，日志还写着 "Using RAG semantic search"。附带查清：语义检索在本机本来也用不了（DeepSeek 没有 /embeddings 接口，日志里 "Failed to generate embedding"），而索引只在「新建划线」这一条 IPC 路径写入，**微信读书导入的 934 条划线从未被索引**，`rebuildIndex()` 也没有任何调用方 —— 也就是说关键词检索一直是唯一的上下文来源，而它是坏的。修完三处后，中文提问能真正检索到自己的划线。新增 12 条测试（`tests/rag-keyword-search.test.ts`）钉住中文命中行为。测试 875→888（41 文件） | AI Agent（接手） |
| 2026-09-16 | **交互逻辑审计（4 个并行子代理 × 全页面）** —— 起因是用户点名：「交互逻辑有没有问题，特别是一个页面俩三个按钮都指向一个功能的」。审计出的问题分四类，**共修 40 余处**：① **重复入口**：首页 hero 三个按钮两个与导航重复、书架页**三个**「同步」按钮同一个函数、书籍详情「在微信读书打开」同屏两次、顶栏刷新按钮与通知面板「立即同步书架」同一个 handleSync、账户页两个一模一样的开关、智能体页「保存配置」=「保存模板」、关于页「问题反馈」=「常见问题」、方法论「开始练习」两个入口、统计页点当前时段 = 点刷新；② **名不副实**：顶栏「今日复习」跳去没有复习入口的知识卡片页、「刷新数据」实际是全量同步、首页「同步微信读书」只跳设置页、搜索框说能搜笔记卡片其实只搜书名、档案页「分享」只是复制文本；③ **假控件与假数字**（最严重）：设置页存储用量 12.3/45.2/128.5 MB 是**写死的常量**（现已改为向主进程要真实文件大小，量不出显示「—」）、微信读书页 **13 个空控件**（同步范围/同步分类/自动化开关，其中「仅同步所选分类的书籍」是写在界面上的假承诺）、知识卡片「反思」筛选永远 0 结果、没有实现的「更换头像」死按钮、「已是最新版本」写死徽章、清理历史写「共 1,284 条」而实际删的是全部 AI 对话；④ **数据风险**：「重新蒸馏/重新提取」对每张卡都是纯 INSERT、**没有任何 delete** —— 对已有内容的书再点一次会成倍翻（现已支持 replace：AI 成功后才清空旧数据，且界面先弹确认）。另修：对话「清空历史」「删除会话」补二次确认、每条 AI 回复的「重新生成」其实只重跑最后一条（现只保留在最后一条回复上）、收起搜索框不清关键词、复习完成态键盘可静默重评、`data-dom-id` 冲突。测试 875 用例（40 文件）不变 | AI Agent（接手） |
| 2026-09-16 | 每日学习重做（**用户原话：「这些真的能做吗，不能做只是形式的删去」**）—— ① 修掉任务标题前凭空出现的「0」：sql.js 读出来的 `is_read` 是数字 0/1，类型却写着 boolean，于是 `{task.done && <Icon/>}` 在未读时求值为数字 0 并被 React 当文本画出来；在边界处归一化成真 boolean（`normalizeArticle`）② **砍掉 4 项纯形式任务**：整理今日笔记 / AI 对话：探讨今日阅读内容 / 写卡片笔记 2 张 / 总结反思今日（点一下弹 toast 说已完成）—— 它们的共同点是没人知道你做没做 ③ 清单只留 4 类**系统自己知道做没做**的事：阅读（`articles.is_read`）/ 复习（真实卡片队列 actionable）/ 生词（`last_review_at`）/ 对话（`conversations.updated_at`）④ 顺带修两处错：旧的「复习 12 张卡片」取的其实是**生词**到期数却写着卡片、还跳到卡片页 —— 现在用真正的卡片队列并跳复习页 ⑤ 删掉手点勾（localStorage 覆盖），勾本身改成纯状态点，进度环不再能被点出来 ⑥ 左卡片「已用时间 / 预计剩余」是把写死的 30/15/10 分钟加起来编出来的，换成真实的「今天已复习 N 张 / 今天已阅读 N 分钟」⑦ 规则抽成纯模块 `src/shared/daily-tasks.ts`，15 条测试钉住「不许再出现没有判定依据的任务」。测试 860→875（40 文件） | AI Agent（接手） |
| 2026-09-16 | 数据血缘修复（**量真实数据库找出来的 5 个断点，全部修完**）—— 方法：先查 `%APPDATA%\zhixing-reader\zhixing.db`，列「应该有的 vs 实际有的」，差值为 0 的字段去代码里找断点。① 934 条划线章节名 **0/934**：`fetchAllContent` 明明取了 `chapters` 对照表，三个导入入口全丢掉了；新增 `src/shared/weread-content.ts`（章节名解析只留一份）+ `import-weread-content.ts`（导入逻辑只留一份，改为 upsert 补空）② 90 张知识卡片来源划线 **0/90**：提示词从没问过"来自第几条"，写入时写死 null；新增 `sourceIndex` 让 AI 回答来源并在**每批内**换算成真实划线 id（分批偏移坑）③ 对话意图 **0/21**：orchestrator 算出来了只写日志，现在随 retrieval done 事件下发并落库 ④ 对话引用来源 **0 条**：`RAGSource.chunkId` 是 **Qdrant 时代**的字段名（Qdrant 早已移除），且 builder 把 bookId/highlightId/relevanceScore 全丢了；`shared/types.ts` 新增唯一真值 `RagSourceRef` 一路串到渲染层 ⑤ `daily_stats.reading_time` 恒为 0：ADD_READING_TIME 通道/handler/repository 全在，渲染层从未调用；改为从微信读书 `/readdata/detail?mode=monthly` 同步（累加改覆盖，月度是全量快照）。另修：`getRetrievability` 与 ts-fsrs 逐点对齐、测试 fixture 复用生产 schema。**再补一层**：五个断点修好的只是"以后"，历史数据不会自己变好，而回填入口都藏在设置页按钮里 —— 新增 `electron/services/startup-repair.ts`，应用一打开自动补卡片来源（纯本地）、划线章节名（先查缺口，**没缺口时零请求**，12 小时节流）、阅读时长（6 小时节流），失败不影响启动。启动实测：44 张卡片补上来源、927/934 划线补上章节名（余 7 条正文为空、按定义补不了，已排除以免反复重拉）、阅读时长写入 7 天 3252 秒。测试 790→875（40 文件） | AI Agent（接手） |
| 2026-09-15 | 算法数字换人话（**"我很多也看不懂"的直接回应**）—— 新增 `src/shared/fsrs-voice.ts`：把 FSRS 状态翻译成「一句陈述 + 一个动作」（`describeForgetting` / `describeNextReview`），全应用共用一张嘴。三条硬约束写进测试逐条守住：① 只说结论不请用户评判算法（否则评分信号被污染，而那是 FSRS 唯一真值输入）② 没数据就闭嘴绝不编日期 ③ 不许用亲切的词撒谎（12 天不能说成"这周"）。界面改动：复习页卡片背面「记忆稳定性 46.35 天 / 当前保持率 87%」→「已经拖了 4 天没复习 · 现在花 10 秒？」；评分后「掌握度 32 → 41」→「好，我 12 天后再来问你」；完成态「平均稳定性 12.3 → 46.4 天」→「最远的一张能记到 9月28日」；生词本抽屉两个数字合并成一句。原始数字收进「为什么这么说」折叠，想核对的人随时能展开。测试 764→790（34 文件） | AI Agent（接手） |
| 2026-09-15 | 每日新卡上限（**修复"用户为什么不每天打开"的真正原因**）—— 实测用户数据库发现 934 张卡片里只有 10 张被复习过，931 张已逾期堆在三个过去的日期上：微信读书同步把全部划线一次性变成"今天就到期"的卡片，而 getDueCards 是 WHERE due <= now，**全项目没有任何每日上限**。用户每次打开复习页看到"931 张待复习"，一个永远做不完的清单。修复：新卡（state=0）与复习卡分开排队，新卡每天最多放 newCardsPerDay 张（默认 15，可设 0 暂停）；getReviewStats().due 语义修正为只统计已学过且到期的卡（900+ 那个数字的来源）；新增 CARDS.GET_QUEUE_STATS 全链路与首页"今天：复习 3 张 · 新卡 15 张"展示；设置页新增「每日新卡上限」。测试 741→764（33 文件），src/shared/study-limits.ts 100% 覆盖 | AI Agent（接手） |
| 2026-09-15 | 生词本三个正确性缺陷 + 测试 schema 去重 — ① **评分档位错位**：界面用 SM-2 风格 1/3/4/5 传评分，主进程映射表 {1:1,2:2,3:3,4:3,5:4} 把「困难」(3) 记成 Good(3) → ts-fsrs 的 Hard 档在生词本里完全不可达；统一为「界面直接传 Rating 1-4」并删除映射表 ② 新词自举写死 Rating.Good → 刚学就忘和轻松想起拿到相同初始状态 ③ 毕业时无条件重新自举 → 冲掉已累积的稳定性（连续复习 6 次 stability 恒为 2.3065，间隔长不起来） ④ is_mastered 改为仅用户显式设置（原复习满 5 次自动置位会让词被 getDueForReview 永久排除） ⑤ 生词本掌握度由 familiarity_level 代理改为 FSRS 推导（原显示 80%「已掌握」而真实分数 29） ⑥ **测试 fixture 复用生产的 applySchemaAndMigrations()**，删掉 280 行复制粘贴的平行 DDL（该漂移已两次导致 "no such column"）⑦ 测试 733→741 | AI Agent（接手） |
| 2026-09-15 | 复习闭环「看得见」—— 卡片掌握度 — ① 新增 `src/shared/fsrs-metrics.ts`（main/renderer 双端复用的纯函数）：`getCardMastery()` 由 FSRS 的 stability/difficulty/reps/lapses 推导 0-100 掌握度，`getRetrievability()` 复刻 FSRS-6.0 遗忘曲线并与 ts-fsrs 逐点校验一致 ② 复习页显示掌握度徽标 + 当前保持率，评分后给出「掌握度 X → Y」即时反馈，完成态展示本轮真实统计（复习张数 / 平均稳定性 A→B / 掌握升降）③ 书籍详情卡片列表补上划线摘要与掌握度徽标（原只有「卡片 #a1b2c3」）④ 修正 `card.review` 的返回类型（原声明 `Promise<Review>`，实际是 `{ reviewId, card }`）⑤ 按 B11 决策树**砍掉**不可达的 `CARDS.UPDATE_MASTERY_LEVEL` / `UPDATE_APPLICATION_TAG` 全链路（通道+handler+preload+repository），DB 两列保留但不再写入 ⑥ 修复 `npm run test:cov` —— `@vitest/coverage-v8` 锁在 2.0.0 与 vitest 2.1.9 不匹配导致覆盖率命令直接崩，重新生成 lock 锁到 2.1.9 ⑦ 测试 703→733（32 文件），覆盖率 90.4/83.5/93.0/90.4 全过阈值 | AI Agent（接手） |
| 2026-09-11 | FSRS 真值对齐 — ① ts-fsrs@5.4.1 实测实现的是 **FSRS-6.0 / 21 参数**（库内 `FSRSVersion` = "using FSRS-6.0"，`default_w.length` = 21，`FSRS6_DEFAULT_DECAY` = 0.1542），全仓库 "FSRS v5 / 19 组权重 / Anki 23.10+ / `(1+factor·t/9S)^decay`" 表述校正 ② **修复词汇间隔恒为 1 天**：`_nextIntervalVocabulary` 公式符号写反（`(1/R)^(1/decay)-1` 恒为负 → 被 clamp 到 1），且把 SM-2 的 efFactor 当记忆稳定性用；现词汇与划线卡片共用同一 ts-fsrs 实例，记忆状态持久化于 vocabulary 表新增的 stability/difficulty/lapses 三列 ③ 修 `w` 长度 <19 被静默忽略（17/19/21 均可下发，与库 checkParameters 对齐）、`getParameters().w` 由 slice(0,17) 恢复为完整 21 ④ 测试 698→703 | AI Agent（接手） |
| 2026-09-11 | 接手核验校准 — 修正 Qdrant→Vectra、fsrs-engine v1→v5 适配层、database/ 文件数、测试数（667→688）、覆盖率门禁口径（未接入）、`.claude/` 与 `.learnings/STANDARDS.md` 标注未落地、`.trae/` 四件套标注缺失；删除 §9.3「项目无测试框架」错误陈述 | AI Agent（接手） |
| 2026-09-11 | 遗留问题治理 — ① 补全 **Skill 导出全链路**（新增 `SKILL.EXPORT_FILE` 通道 + 方法论详情页「导出为 Skill」按钮，此前 handler/preload/AI 服务/测试齐全但无 UI）② 删除 6 个指向已消失 `scripts/` 的死脚本 ③ 清理 eslint 失效 grandfather 条目 ④ 明确 `.learnings/`/`.workbuddy/memory/` 为**本地文件不入库**（含内部策略与已知问题，不进公开仓库）⑤ `diagrams/`、`html2pdf-ultra.js` 显式 gitignore ⑥ CI 移除永不产出的 coverage artifact 步骤 | AI Agent（接手） |
| 2026-08-28 | v1.1.0 维护迭代 — 换机恢复 + 去伪存真（假数据/死链治理）+ 间隔复习与 Token 统计落地 + 画像注入 + database/ipc 拆分 + 编排页迁入设置壳层 + 管理后台移出前端；端口勘误 5275→5500 | AI Agent |
| 2026-07-20 | 初始化（v1）— 加入 .claude/、CI、Vitest、AGENTS.md | AI Agent |
| 2026-07-21 | 死代码治理循环工程收尾 — 新增第九章"死代码治理经验" + 7 个 IPC 通道清单 + 7 维质量评分基准 | dead-code-governance verifier-subagent |
| 已处理 | husky pre-commit hook 安装（2026-09-18：lint+typecheck+test 三连）| — |
| 已处理 | commitlint 配置（2026-09-18：config-conventional + commit-msg 钩子，R10 不再靠人工）| — |
| 已处理 | ~~`.claude/rules/*` + `.learnings/STANDARDS.md` 正本~~ —— 2026-09-24 收掉这些指针：§5.3 表即为 15 条规则正本，§六 改为写"每个领域由哪份配置强制"，不再另立规范文本（Issue #7）| — |
| 已处理 | ~~`.trae/specs/dead-code-governance/` 四件套~~ —— 2026-09-24 从 §9.4 删除，经验唯一沉淀位置改为 `.learnings/LEARNINGS.md`（Issue #7）| — |
| 已处理 | `scripts/` 相关 6 个死脚本已从 `package.json` 移除（2026-09-11）| — |
