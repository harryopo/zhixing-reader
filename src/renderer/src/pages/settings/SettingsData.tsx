import PlaceholderPage from '../PlaceholderPage'

export default function SettingsData() {
  return (
    <PlaceholderPage
      title="数据"
      subtitle="数据导出、导入、备份与清理"
      hint="数据设置页面将在阶段2-P3 按设计稿 settings-data.html 实现，包括导出 SQLite/JSON、导入备份、清空缓存等。"
    />
  )
}
